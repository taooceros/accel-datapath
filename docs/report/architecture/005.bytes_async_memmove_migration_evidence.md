# Bytes async memmove migration evidence index

Status: T01 validation-index draft for M006/S04; T02 must refresh command outcomes.
Date: 2026-04-28

## Scope

This report is the operator-facing validation index for the M006 bytes-based async memmove migration. It ties R001 to the active API, stale-reference guards, host-free contract tests, downstream proof surfaces, and operational verifier classifications that must be refreshed in T02.

This report is not a raw transcript dump. It names the commands that produce proof, the active files those commands cover, the interpretation rules for pass versus `expected_failure`, and the artifact/log paths a future agent should inspect after the commands run.

## R001 evidence mapping

| R001 claim | Active proof surface | Focused command evidence | Interpretation |
|---|---|---|---|
| Public async request construction is bytes-based, source-first, and caller-owned. | `idxd-rust/src/async_session.rs`, `idxd-rust/README.md`, `idxd-rust/tests/async_memmove_contract.rs` | `cargo test -p idxd-rust --test async_memmove_contract --test tokio_handle_contract --test async_validation_cli_contract --test async_verifier_contract` | Host-free contract tests must prove canonical `AsyncMemmoveRequest::new(Bytes, BytesMut)` construction, explicit destination ownership, validation-before-enqueue behavior, and diagnostic preservation. |
| Removed helper semantics are guarded rather than kept as compatibility aliases. | `idxd-rust/scripts/verify_package_inventory.sh`, `idxd-rust/scripts/verify_async_memmove_contract.sh`, focused stale-surface tests | `bash idxd-rust/scripts/verify_package_inventory.sh`; `idxd-rust/scripts/verify_async_memmove_contract.sh` | Guards must reject old package names and stale async helper shapes while allowing the canonical bytes constructor and legitimate validation/report metadata. |
| Downstream integration can use the corrected public surface. | `accel-rpc/tonic-profile/src/bin/downstream_async_handle.rs`, `accel-rpc/tonic-profile/README.md`, `accel-rpc/tonic-profile/tests/downstream_async_handle_contract.rs` | `cargo test -p tonic-profile --test downstream_async_handle_contract`; `cargo check -p tonic-profile --bin downstream_async_handle` | Downstream proof must construct owned `Bytes` plus `BytesMut` requests, compose cloned handles with Tokio, and inspect `AsyncMemmoveResult.destination` plus `AsyncMemmoveResult.report`. |
| Operational scripts classify prepared-host pass versus ordinary-host blocker truthfully. | `idxd-rust/scripts/verify_async_memmove.sh`, `accel-rpc/tonic-profile/scripts/verify_downstream_async_handle.sh` | `bash idxd-rust/scripts/verify_async_memmove.sh`; `bash accel-rpc/tonic-profile/scripts/verify_downstream_async_handle.sh` | A prepared host may produce `verdict=pass`. An ordinary host may produce `verdict=expected_failure` only when the script exits successfully and records explicit phase/kind metadata and artifact/log paths. |

## Canonical API proof

The canonical public construction text for this migration is:

```rust
AsyncMemmoveRequest::new(Bytes, BytesMut)
```

The source buffer is first and is owned as `Bytes`. The destination buffer is second and is caller-supplied as `BytesMut`. The result surface returns explicit destination ownership alongside validation metadata:

```rust
AsyncMemmoveResult {
    destination: BytesMut,
    report: MemmoveValidationReport,
}
```

The validation package must prove the active source-first constructor and result ownership through code, docs, tests, and downstream consumer checks. It must not imply a source-only constructor, a public allocation convenience constructor, a compatibility alias, or a hidden copy-back helper.

## Stale-reference and package guard proof

`idxd-rust/scripts/verify_package_inventory.sh` is the focused stale-reference guard. It checks these categories:

- `package-owner`: rejects stale package-owner references such as old `dsa-ffi`, `iax-ffi`, or binding-name variants outside historical/guard contexts.
- `old-async-api`: rejects removed active async surfaces such as `copy_exact`, `copy_into`, `with_destination_len`, one-argument `AsyncMemmoveRequest::new(...)`, `source_bytes()`, `destination_bytes()`, `result.bytes`, and public `memmove_into` call shapes outside allowed stale-reference tests or guard text.
- `source-only-request-shape`: rejects active source-only request wording or `Vec<u8>` request-shape remnants outside historical/guard contexts.
- `dst-len-side-channel`: rejects destination-length side-channel language when it represents the old async request shape, while allowing validation/report fields that use source and destination lengths to explain errors.
- `handwritten-dsa-abi`: rejects handwritten DSA ABI structs outside the canonical binding layer.

`idxd-rust/scripts/verify_async_memmove_contract.sh` checks the normative report `docs/report/architecture/004.bytes_async_memmove_contract.md` for the bytes constructor, result shape, stale-surface rejection, destination initialization and unsafe-boundary guidance, inline `enqcmd` v1 policy, failure taxonomy, and deferred future-work boundaries.

## Host-free contract tests

The host-free proof commands must not depend on a prepared DSA machine. They exist to catch API and schema regressions before any live verifier is interpreted:

- `cargo test -p idxd-rust --test async_memmove_contract --test tokio_handle_contract --test async_validation_cli_contract --test async_verifier_contract`
- `cargo test -p tonic-profile --test downstream_async_handle_contract`
- `cargo check -p idxd-rust --bins`
- `cargo check -p tonic-profile --bin downstream_async_handle`

These checks should prove compile-time and schema-level facts: canonical construction, explicit result ownership, validation error preservation, Tokio handle composition, verifier artifact schema validation, downstream package identity, proof seam fields, and stale-helper rejection. They do not prove that hardware ran.

## Downstream integration proof

The downstream proof seam is `tonic-profile`'s standalone `downstream_async_handle` binary and its wrapper script. It proves a package outside `idxd-rust` can use the public async owner/handle surface from ordinary Tokio code.

The downstream artifact contract includes:

- `proof_seam=downstream_async_handle`
- `consumer_package=tonic-profile`
- `binding_package=idxd-rust`
- `composition=tokio_join`
- `operation_count=2`
- typed lifecycle, worker, validation phase, and validation error fields

This is intentionally separate from `idxd-rust`'s crate-local `await_memmove` proof. It is also separate from `tonic-profile/src/custom_codec.rs`, which remains a synchronous codec seam and must not be treated as the async integration seam.

## Operational verifier semantics

The live-oriented verifier scripts are the only supported operational entrypoints for this evidence package. Do not run the hardware-facing proof binaries directly for final evidence because the wrappers add launcher/preflight classification and artifact validation.

| Script | Binary wrapped | Artifact contract | Pass interpretation | Expected-failure interpretation |
|---|---|---|---|---|
| `bash idxd-rust/scripts/verify_async_memmove.sh` | `await_memmove` | JSON artifact plus stdout/stderr paths containing `ok`, device, requested bytes, page-fault retries, final status, phase, error kind, lifecycle/worker/validation fields, and message. | `verdict=pass` means the launch-wrapped async proof completed on a prepared host and the artifact passed schema/consistency checks. | `verdict=expected_failure` is acceptable only for explicitly classified preflight or runtime failures, with `failure_phase`, launcher status or typed error fields, and artifact/log paths. |
| `bash accel-rpc/tonic-profile/scripts/verify_downstream_async_handle.sh` | `downstream_async_handle` | JSON artifact plus stdout/stderr paths containing downstream seam/package/composition/operation-count fields and typed failure fields. | `verdict=pass` means the downstream Tokio proof completed on a prepared host and the artifact passed schema/consistency checks. | `verdict=expected_failure` is acceptable only for explicitly classified ordinary-host or typed runtime failures, with `failure_phase`, launcher status or typed error fields, and artifact/log paths. |

Malformed artifacts, missing artifacts, stdout/artifact divergence, contradictory success/failure fields, timeout without a classified artifact, or non-zero verifier exit status are blocking evidence failures.

## Ordinary-host classification rules

The report separates API-migration proof from prepared-host hardware proof.

Acceptable ordinary-host outcomes:

- The verifier exits with status `0`.
- The final verifier line includes `verdict=expected_failure`.
- The failure is explicitly classified with `failure_phase=preflight` or `failure_phase=runtime`.
- Preflight blockers include concrete launcher/device status such as `missing_work_queue`, `missing_devenv`, `missing_launcher`, or `missing_capability`.
- Runtime expected failures include typed lifecycle, worker, or validation fields rather than an unstructured crash.
- The final line preserves raw artifact/stdout/stderr paths where applicable.

Unacceptable outcomes:

- Treating `expected_failure` as prepared-host success.
- Treating direct proof-binary output as final evidence without the wrapper.
- Treating a missing, empty, malformed, or internally contradictory artifact as acceptable.
- Treating timeout, unclassified launcher failure, or non-zero verifier exit as ordinary-host proof.

## Command evidence table for T02

T02 must replace the status cells below with fresh observed exit status, concise interpretation, and concrete artifact/log paths when the command emits them. Until then, these rows are the planned evidence index, not final command results.

| Command | Status for T02 refresh | Interpretation required | Artifact/log path column |
|---|---|---|---|
| `bash idxd-rust/scripts/verify_package_inventory.sh` | Pending refresh in T02 | Must pass stale package/API/ABI guards without disallowed active references. | No JSON artifact; preserve command output in T02 notes if it fails. |
| `idxd-rust/scripts/verify_async_memmove_contract.sh` | Pending refresh in T02 | Must pass against `docs/report/architecture/004.bytes_async_memmove_contract.md` and the matching plan. | No JSON artifact; preserve command output in T02 notes if it fails. |
| `cargo test -p idxd-rust --test async_memmove_contract --test tokio_handle_contract --test async_validation_cli_contract --test async_verifier_contract` | Pending refresh in T02 | Must pass host-free async API, Tokio handle, CLI schema, and verifier contract tests. | Cargo test output; no payload artifact expected. |
| `cargo test -p tonic-profile --test downstream_async_handle_contract` | Pending refresh in T02 | Must pass downstream host-free contract and stale-helper checks. | Cargo test output; no payload artifact expected. |
| `cargo check -p idxd-rust --bins` | Pending refresh in T02 | Must prove crate-local proof binaries compile on the migrated API. | Cargo check output; no payload artifact expected. |
| `cargo check -p tonic-profile --bin downstream_async_handle` | Pending refresh in T02 | Must prove the downstream proof binary compiles against the public bytes API. | Cargo check output; no payload artifact expected. |
| `bash idxd-rust/scripts/verify_async_memmove.sh` | Pending refresh in T02 | Must end in `verdict=pass` on a prepared host or `verdict=expected_failure` with explicit ordinary-host classification. | Final verifier line supplies `artifact=...`, `stdout=...`, and `stderr=...` when it reaches artifact/runtime phases; preflight rows still preserve `output_dir=...` and launcher status. |
| `bash accel-rpc/tonic-profile/scripts/verify_downstream_async_handle.sh` | Pending refresh in T02 | Must end in `verdict=pass` on a prepared host or `verdict=expected_failure` with explicit ordinary-host classification. | Final verifier line supplies `artifact=...`, `stdout=...`, and `stderr=...` when it reaches artifact/runtime phases; preflight rows still preserve `output_dir=...` and launcher status. |

## Downstream assumptions

Downstream consumers may assume:

- The public async request is built from an owned `Bytes` source and an owned caller-supplied `BytesMut` destination.
- The async result returns explicit destination ownership and a validation report.
- v1 submission is inline per logical request or an equivalent explicit worker-owned boundary documented by active code; callers must not depend on hidden software aggregation.
- Operational proof should use wrapper scripts so ordinary-host blockers are classified truthfully.

Downstream consumers must not assume:

- A hidden allocation convenience constructor exists.
- A public `memmove_into` copy-back helper exists.
- Software batching, aggregation, or flush timers exist.
- MOVDIR64 support exists.
- `tonic-profile/src/custom_codec.rs` is the async proof seam.

## Non-claims

This report does not claim live DSA success until a T02 verifier row records `verdict=pass` from the wrapper on a prepared host.

This report does not claim payload contents. It may describe validation of copied destination prefixes, requested byte counts, schema fields, package identity, proof seam identity, and operation counts, but it must not dump source bytes or destination bytes.

This report does not claim throughput, latency, batching behavior, fairness, queue-depth behavior, or MOVDIR64 behavior.

This report does not claim the future borrowed async zero-copy API is designed or safe.

## Future work explicitly deferred

- Software aggregation policy, including queueing, coalescing, backpressure, fairness, and failure mapping.
- Batching policy and any flush timer behavior.
- MOVDIR64 submission support.
- Borrowed async zero-copy or spare-capacity safe wrapper design, including pointer validity, aliasing, cancellation safety, completion safety, and initialized-length rules.
- Restoring real downstream codec/body-stage accounting where prior shimmed instrumentation is insufficient for seam-cost evidence.

## Evidence paths

| Path | Role |
|---|---|
| `docs/report/architecture/004.bytes_async_memmove_contract.md` | Normative bytes async API contract and future-work boundaries. |
| `idxd-rust/README.md` | Crate-local operator documentation for `AsyncDsaSession`, `AsyncDsaHandle`, `AsyncMemmoveRequest`, `AsyncMemmoveResult`, and verifier interpretation. |
| `accel-rpc/tonic-profile/README.md` | Downstream async-handle proof documentation and codec boundary warning. |
| `idxd-rust/scripts/verify_package_inventory.sh` | Stale package/API/ABI guard. |
| `idxd-rust/scripts/verify_async_memmove_contract.sh` | Contract-report mechanical verifier. |
| `idxd-rust/scripts/verify_async_memmove.sh` | Crate-local async operational verifier with launcher/preflight classification and artifact validation. |
| `accel-rpc/tonic-profile/scripts/verify_downstream_async_handle.sh` | Downstream operational verifier with launcher/preflight classification and artifact validation. |
| `idxd-rust/tests/async_memmove_contract.rs` | Host-free bytes async API contract tests. |
| `idxd-rust/tests/tokio_handle_contract.rs` | Host-free Tokio handle composition and lifecycle checks. |
| `idxd-rust/tests/async_validation_cli_contract.rs` | Host-free async proof binary schema/behavior checks. |
| `idxd-rust/tests/async_verifier_contract.rs` | Host-free verifier schema and expected-failure classification checks. |
| `accel-rpc/tonic-profile/tests/downstream_async_handle_contract.rs` | Host-free downstream proof and stale-helper contract checks. |
