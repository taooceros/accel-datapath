# Hardware Rust readability baseline

**Milestone:** M009 — Hardware Rust readability cleanup  
**Slice:** S01 — Inventory post-M008 seams and contract baselines  
**Status:** T03 Tokio benchmark artifact inventory added; later S01 tasks must fill remaining `idxd-rust`, `idxd-sys`, and `hw-eval` inventories while preserving this contract shape  
**Reader:** A maintainer or future agent preparing an M009 readability refactor in the hardware Rust stack.  
**Post-read action:** Identify which `idxd-rust`, `idxd-sys`, and `hw-eval` responsibilities may move in S02-S06, which public/API/artifact contracts protect those moves, and which ordinary-host checks must still pass before a refactor proceeds.

## Purpose

This report is the durable readability baseline for M009/S01. It freezes the shape of the responsibility map and verification contracts before any code-moving or readability refactor begins.

S01 changes documentation and verification baselines only. It does not change runtime behavior, public APIs, DSA/IAX submission semantics, artifact schemas, or prepared-host claim criteria. Downstream slices may improve readability only when their changes preserve the contracts named here or update a deliberately versioned replacement contract with matching tests and verifiers.

The baseline is contract-first: if a future refactor makes a module easier to read but weakens error classification, request recovery, stdout/artifact equality, JSON field stability, ordinary-host expected-failure behavior, or the no-payload diagnostic rule, that refactor is outside the M009 cleanup contract.

## Source inputs

This scaffold is grounded in the post-M008 hardware cleanup reports, the `idxd-rust` operational README, the repository report-writing conventions, and the active requirement set for M009. Later S01 tasks should fill the detailed rows from tracked source and tracked reports only.

Required inputs for the completed S01 baseline:

- M008 migration contract and cleanup-convention reports.
- `idxd-rust` README and verifier scripts.
- `idxd-rust` library, proof-binary, and contract-test surfaces.
- `idxd-rust/src/bin/tokio_memmove_bench.rs`, `idxd-rust/tests/async_benchmark_cli_contract.rs`, `idxd-rust/tests/async_benchmark_verifier_contract.rs`, and `idxd-rust/scripts/verify_tokio_memmove_bench.sh` for the Tokio benchmark artifact baseline.
- `idxd-sys` raw ABI/MMIO boundary and layout tests.
- `hw-eval` benchmark harness, report schema, plotting consumers, and README behavior.
- R018 readability-maintainability requirement context.

Do not use `.gsd/` artifacts, ignored `target/` output, copied payload buffers, or prepared-host-only evidence as fixtures for this host-free baseline.

## Responsibility map

This section will become the maintainer-readable map of what each hardware Rust area owns after M008 and before M009 refactors begin.

| Area | Current responsibility | Candidate readability pressure | Contract that must stay stable | S02-S06 owner |
|---|---|---|---|---|
| `idxd-rust` public validation/session surface | Safe sync and async DSA memmove APIs, validation reports, typed failure classification, request recovery, and proof binaries. | Separate public API contracts from direct-runtime internals and proof-binary plumbing without changing caller-visible behavior. | Public constructors/accessors, error classifications, request recovery, proof artifact fields, stdout/artifact equality, and no-payload diagnostics. | S02, S03, S04 |
| `idxd-rust` direct async runtime | Direct ENQCMD-oriented async submission, completion monitoring, retry/backpressure metadata, and operation ownership. | Make runtime ownership and completion-state transitions easier to follow without redesigning submission semantics. | Accepted-operation ownership, completion snapshot metadata, direct failure kinds, bounded retry/backpressure behavior, and destination-length safety. | S03, S04 |
| `idxd-sys` raw boundary | Bindgen-backed DSA/IAX descriptors, completion records, portal mapping, raw submission helpers, topology helpers, and OS error surfaces. | Clarify raw-boundary comments and module organization without wrapping low-level hardware signals in higher-level abstractions. | Raw ABI layout, unsafe preconditions, OS `io::Error` sources, status bytes, and transparent descriptor/completion behavior. | S05 |
| `hw-eval` benchmark harness | CLI-driven DSA/IAX/software benchmark execution, JSON report emission, plotting inputs, and operational diagnostics. | Separate normalized config/reporting concerns from hot-loop benchmark code while preserving artifact shape. | CLI flags, report fields, benchmark names, graphing assumptions, non-fatal pin behavior, prepared-host launch expectations, and no-payload diagnostics. | S06 |
| Downstream proof consumers | Repo-local verifier scripts, contract tests, plotting scripts, and downstream async-handle proof paths that consume hardware Rust APIs or artifacts. | Keep compatibility checks visible before moving symbols or reshaping modules. | Required fields, final-line diagnostics, expected-failure classification, claim eligibility, and forbidden payload-content checks. | S02-S06 |

## Direct async S02 inventory

Reader for this section: a maintainer preparing S02's `idxd-rust` direct async readability refactor. Post-read action: move direct async code along the named seams while preserving the host-free contracts and without claiming prepared-host hardware success from ordinary-host evidence.

### Direct async responsibility rows

| Phase | Current owner | Planned downstream slice | Safe move candidates | Preserve-by-default contracts | Exact verification commands |
|---|---|---|---|---|---|
| Public async owner/session surface | `idxd-rust/src/async_session.rs` owns `AsyncDsaSession`, `AsyncDsaHandle`, the cloneable handle lifecycle, owner shutdown state, public `open`/`open_config`/`handle`/`memmove`/`shutdown` entry points, and the hidden fixture drivers used by host-free tests. | S02 | Separate public API wiring, hidden fixture-driver adapters, and lifecycle state helpers from request/result/error type definitions if names, constructors, and behavior stay stable. | `AsyncDsaSession`, `AsyncDsaHandle`, `AsyncMemmoveRequest`, `AsyncMemmoveResult`, `AsyncMemmoveError::kind`, `lifecycle_failure_kind`, `worker_failure_kind`, `direct_failure_kind`, `direct_failure`, `memmove_error`, and `into_request` remain public/API-compatible unless explicit versioned tests are added. Owner shutdown must reject new requests with `owner_shutdown` while accepted direct work remains owned by the runtime. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_memmove_contract --test tokio_handle_contract --no-run` |
| Request validation and recovery | `AsyncMemmoveRequest::new` validates owned `Bytes` plus `BytesMut` spare capacity before enqueue and `AsyncMemmoveRequestError` returns typed memmove diagnostics plus rejected buffers. Direct and lifecycle failures use `into_request` or embedded request fields to preserve recovery only when safe. | S02 | Extract constructor/error formatting helpers or recovery helpers only if zero-length and destination-size rejection still happen before runtime dispatch and typed buffer recovery remains available. | Zero-length request rejection, destination-size request rejection, pre-enqueue owner shutdown recovery, worker/direct structural recovery, and accepted terminal hardware failures that must not pretend buffers are reusable. No-payload diagnostics may mention lengths, phases, retry counts, status bytes, and fault addresses but not copied source bytes, destination bytes, buffer dumps, or payload-content examples. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_memmove_contract --no-run` |
| Direct runtime registration and initial submission | `idxd-rust/src/async_direct.rs` owns `DirectAsyncMemmoveRuntime`, the Tokio-runtime requirement, pending operation registry, operation ids, closed flag, submission retry budget, initial descriptor fill, ENQCMD accept/reject handling, and pre-acceptance recovery on registration/backpressure failures. | S02 | Move registration/submission loops into named helpers or submodules if retry budget, `RuntimeUnavailable`, `RegistrationClosed`, and `BackpressureExceeded` metadata stay stable. | Bounded retry/backpressure behavior, retry_count/retry_budget/requested_bytes in `AsyncDirectFailure`, direct failure accessors, `AsyncDirectFailure` as the error source, and no-payload formatting for rejected submissions. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_memmove_contract --no-run` |
| Pending operation lifecycle | `PendingOperation` in `idxd-rust/src/async_direct.rs` owns source and destination buffers, descriptor/completion state, last completion snapshot, reply channel, request recovery, and success finalization until terminal completion or cleanup. | S02 | Extract operation ownership, descriptor access, reply completion, and request recovery helpers if accepted-operation ownership and destination-length safety remain visible. | Dropped awaiters must not remove accepted operations before completion; monitor close must resolve accepted pending work; post-copy mismatch keeps destination length safe and does not return a reusable request; accepted terminal failures do not expose payload bytes. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_memmove_contract --no-run` |
| Monitor polling and completion dispatch | `monitor_completion_records` in `idxd-rust/src/async_direct.rs` scans pending operations, sleeps with idle backoff, yields between polling passes, removes terminal operations, and dispatches snapshots to operation-local classification. | S02 | Move monitor polling into a dedicated monitor helper if futures still resolve only from completion snapshots and out-of-order completions remain per-operation. | Completion snapshots drive resolution, public direct futures wait for completion records rather than submit acceptance, concurrent direct requests can complete out of order, cloned Tokio handles compose with `join!` and spawned tasks, and dropping one clone does not shut down another. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test tokio_handle_contract --no-run` |
| Completion, retry, and recovery | `PendingOperation::handle_snapshot`, `submit_continuation`, `finish_success`, and `recover_request` combine shared memmove completion classification, page-fault retry continuation, retry backpressure failure metadata, post-copy verification, and safe request recovery. `idxd-rust/src/direct_memmove.rs` owns `DirectMemmoveState`, continuation offsets, retry count, snapshot classification, success reports, and initialized-destination verification. | S02 | Separate retry classification, continuation submission, and success verification helpers if `CompletionSnapshot`, retry metadata, page-fault retry behavior, and `DirectMemmoveState` invariants stay unchanged. | Page-fault retry resubmits continuation descriptors, retry failures preserve completion_snapshot metadata, malformed completion snapshots surface typed `MemmoveError::MalformedCompletion`, success reports preserve requested bytes/retry/final-status metadata, and byte-mismatch diagnostics stay no-payload. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_memmove_contract --no-run` |
| Fake/backend seams | `DirectMemmoveBackend`, `DirectPortalBackend`, `AsyncMemmoveWorker`, and `direct_test_support::ScriptedDirectBackend` separate live WQ portal submission from host-free fake ENQCMD and fake completion-record behavior. `direct_async_contract_guard` separately protects the tracked design contract for submit-now/complete-later direct async. | S02 | Move test-support backend code or hidden legacy worker fixtures only if public defaults still use direct runtime and host-free tests can still inject deterministic accept/reject/completion behavior. | Public `open`/`open_default`/`open_with_retries` must not silently fall back to blocking worker execution; fake completion-record transitions, fake ENQCMD accept/reject behavior, fake retry completion snapshots, dropped-receiver cleanup, shutdown classification, and destination-length assertions remain available without hardware. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test direct_async_contract_guard -- --nocapture` |

### Direct async contract tests

- `idxd-rust/tests/async_memmove_contract.rs` guards owned request construction, zero-length and destination-size rejection, queue-open metadata, worker structural failures, owner shutdown, request recovery, direct completion snapshots, retry/backpressure metadata, malformed completion metadata, dropped awaiter behavior, post-copy destination-length safety, and no-payload direct failure formatting.
- `idxd-rust/tests/tokio_handle_contract.rs` guards cloneable Tokio handle composition, out-of-order direct completions, spawned-task independence, clone lifetime behavior, explicit owner shutdown, accepted-work survival, and `owner_shutdown` classification for post-shutdown submissions.
- `idxd-rust/tests/direct_async_contract_guard.rs` guards the tracked direct async design report/verifier so stale claims such as MOVDIR64 fallback, blocking worker future resolution, preallocated-registry assumptions, tonic dependency claims, or payload byte logging cannot re-enter the baseline.

### Direct async public/API freeze

S02 must treat these names as stable unless it adds explicit versioned compatibility tests and updates this baseline deliberately: `AsyncDsaSession`, `AsyncDsaHandle`, `AsyncMemmoveRequest`, `AsyncMemmoveResult`, `AsyncMemmoveRequestError`, `AsyncMemmoveError`, `AsyncDirectFailure`, `AsyncDirectFailureKind`, `AsyncLifecycleFailureKind`, `AsyncWorkerFailureKind`, `DirectAsyncMemmoveRuntime`, `DirectMemmoveBackend`, `DirectPortalBackend`, and `direct_test_support::ScriptedDirectBackend`.

The accessor contract is also frozen: request length/capacity accessors, `AsyncMemmoveRequest::into_parts`, request-error `into_parts`, `AsyncMemmoveError::kind`, lifecycle/worker/direct failure accessors, direct failure `requested_bytes`, `retry_budget`, `retry_count`, `completion_snapshot`, and request recovery through `into_request` must not change behavior without explicit tests.

### Ordinary-host and prepared-host direct async claims

Ordinary-host checks may claim that the direct async code preserves public API shape, request validation, host-free fake backend behavior, lifecycle classification, bounded retry/backpressure metadata, completion snapshot propagation, no-payload diagnostics, and documented contract wording. Ordinary-host checks must not claim prepared-host hardware success, real ENQCMD throughput, real DSA/IAX completion success, or performance behavior.

Prepared-host success remains limited to launch-wrapped hardware verifier scripts and real work-queue runs. Host-free tests can keep S02 honest before refactoring, but any statement that direct async submission completed on DSA/IAX hardware requires prepared-host evidence.

## Tokio benchmark S03 inventory

Reader for this section: a maintainer preparing S03's `idxd-rust` Tokio benchmark artifact readability refactor. Post-read action: move benchmark CLI, execution, artifact, and verifier code along the named seams while preserving software diagnostic proof, prepared-host hardware claim criteria, stdout/artifact equality, and no-payload reporting.

### Tokio benchmark responsibility rows

| Phase | Current owner | Planned downstream slice | Safe move candidates | Preserve-by-default contracts | Exact verification commands |
|---|---|---|---|---|---|
| CLI and config parsing | `tokio_memmove_bench` owns argument parsing for `--device`, `--backend`, `--suite`, `--bytes`, `--iterations`, `--concurrency`, `--duration-ms`, `--format`, `--artifact`, and `--help`, including bounded numeric validation and enum validation before benchmark execution. | S03 | Extract parser/config helpers if invalid numeric, enum, empty-device, missing artifact-value, directory artifact-path, and unwritable artifact-path behavior stays identical. | Help text, exit code 2 for parse failures, bounded ranges, default device/backend/suite/format, `MemmoveRequest` validation for `--bytes`, and no hardware touch for invalid CLI inputs. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_cli_contract -- --nocapture` |
| Backend, suite, and mode selection | `tokio_memmove_bench` maps `backend=software|hardware`, `suite=canonical|latency|concurrency|throughput`, and benchmark modes `single_latency`, `concurrent_submissions`, and `fixed_duration_throughput` to result rows. Software uses `software_direct_async_diagnostic`; hardware uses `direct_async` plus a `direct_sync` comparison row for canonical and latency. | S03 | Move backend/suite/mode dispatch into named modules if row target names, mode membership, comparison-target semantics, and host-free software behavior stay unchanged. | `backend`, `suite`, `mode`, `target`, `comparison_target`, software diagnostic target, hardware `direct_async`, hardware `direct_sync`, and canonical row coverage are preserve-by-default artifact fields. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_cli_contract --test async_benchmark_verifier_contract -- --nocapture` |
| Software diagnostic execution | `SoftwareDirectBackend` and `software_artifact` provide a host-free direct-async diagnostic path that publishes completion metadata through the same artifact schema without claiming hardware proof. | S03 | Extract software backend or diagnostic runner helpers if software mode remains deterministic, host-free, current-thread Tokio based, and non-claim-eligible. | Software artifacts and rows must keep `claim_eligible=false`, target `software_direct_async_diagnostic`, `runtime_flavor=current_thread`, `worker_threads=1`, positive operation/latency/rate fields for passing rows, and null failure metadata on pass. Software diagnostic proof must never be described as prepared-host hardware success. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_cli_contract -- --nocapture` |
| Hardware execution and claim gating | `hardware_artifact`, `run_async_mode`, and `run_sync_comparison` own prepared-host async/direct-sync row generation and classify queue-open, validation, direct, lifecycle, worker, join, and sync failures into artifact fields. | S03 | Move hardware runner and failure-row construction if prepared-host claim criteria and failure classifications remain visible to tests and verifiers. | Prepared-host claims require top-level `verdict=pass`, `ok=true`, `claim_eligible=true`, all required `direct_async` rows passing, and the required `direct_sync` comparison row for canonical/latency. Hardware expected failures keep `claim_eligible=false` and machine-readable `failure_class`, `error_kind`, `direct_failure_kind`, `validation_phase`, and `validation_error_kind`. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_verifier_contract -- --nocapture` |
| Artifact serialization and schema | `BenchmarkArtifact`, `BenchmarkResult`, `emit_artifact`, and `render_text` own JSON/text serialization, top-level fields, result rows, and stdout rendering. | S03 | Split schema structs or renderers if public artifact names, nullable failure metadata, result row shape, and text summary fields remain stable or get an explicit versioned replacement test. | `schema_version`, `ok`, `verdict`, `device_path`, `backend`, `claim_eligible`, `suite`, `runtime_flavor`, `worker_threads`, `requested_bytes`, `iterations`, `concurrency`, `duration_ms`, top-level failure metadata, `results`, result row latency/rate fields, result row failure metadata, and row `claim_eligible` are preserve-by-default. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_cli_contract --test async_benchmark_verifier_contract -- --nocapture` |
| Artifact path handling and output writing | `validate_artifact_path`, `temporary_artifact_path`, `write_artifact`, and `emit_artifact` validate artifact destinations, write through a temporary path, atomically rename, and print exactly the emitted artifact to stdout. | S03 | Move writer/path helpers if artifact-path validation, temporary file naming, atomic commit behavior, and stdout/artifact byte-for-byte equality remain protected. | `--artifact` must reject missing values and directory paths before benchmark execution; written artifact text must match stdout exactly; malformed JSON and stdout/artifact mismatch are hard verifier failures. | `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_cli_contract --test async_benchmark_verifier_contract -- --nocapture` |
| Verifier semantics and expected failures | `idxd-rust/scripts/verify_tokio_memmove_bench.sh` owns build/preflight/runtime/artifact-validation phases, software diagnostic verification, launcher capability preflight, expected-failure reporting, schema validation, row validation, and final `phase=done` summaries. `idxd-rust/tests/async_benchmark_verifier_contract.rs` freezes these outcomes. | S03 | Refactor verifier functions or Python validation blocks if final-line semantics, expected-failure classification, and hard artifact-validation failures stay stable. | Missing work queue, missing `devenv`, missing launcher, and missing launcher capability are expected preflight failures. Classified hardware runtime failures are expected runtime failures. Invalid backend/suite, timeout, malformed JSON, missing required schema fields, stdout/artifact divergence, software `claim_eligible=true`, missing hardware sync comparison, and forbidden payload dump fields are hard failures. | `bash idxd-rust/scripts/verify_tokio_memmove_bench.sh` on prepared hosts; `cargo test --manifest-path ./Cargo.toml -p idxd-rust --test async_benchmark_verifier_contract -- --nocapture` on ordinary hosts |

### Tokio benchmark public artifact freeze

S03 must treat the benchmark artifact as preserve-by-default. The top-level schema fields `schema_version`, `backend`, `suite`, `claim_eligible`, `verdict`, `ok`, `failure_class`, `error_kind`, `direct_failure_kind`, `validation_phase`, `validation_error_kind`, direct retry metadata, completion metadata, and `results` must not be renamed or removed without an explicit versioned replacement contract.

Each result row preserves `mode`, `target`, `comparison_target`, request knobs, operation counts, elapsed/latency/rate metrics, row verdict, row failure classifications, direct/validation metadata, completion metadata, and row `claim_eligible`. Passing rows require completed operations and no failure metadata. Failing rows require non-empty failure classification and error kind.

Stdout/artifact equality is part of the public contract: when `--artifact` is supplied, the file content must match stdout exactly after trimming verifier-captured newlines. Verifier consumers should parse `verify_tokio_memmove_bench.sh` final `phase=done` lines and artifact fields instead of inferring validity from filenames or process exit alone.

### Software diagnostic and prepared-host benchmark claims

Software diagnostic mode is useful ordinary-host evidence that the Tokio benchmark CLI, async row generation, schema rendering, artifact writing, and verifier validation still work. It is never a hardware-performance or DSA/IAX success claim, so software top-level and row `claim_eligible` must remain `false`.

Prepared-host hardware claims require launch-wrapped verifier evidence, a successful hardware artifact, `claim_eligible=true`, the expected `direct_async` rows for the selected suite, and a `direct_sync` comparison row for canonical or latency suites. Missing launcher prerequisites and classified queue-open/runtime failures remain `expected_failure` outcomes, not broken contracts, while malformed artifact/schema/parity results are contract blockers.

### Tokio benchmark negative and redaction baseline

The host-free negative baseline names invalid numeric CLI inputs, invalid enum values, empty device paths, missing artifact values, directory artifact paths, missing or contradictory verifier overrides, malformed JSON, missing required schema fields, stdout/artifact mismatches, software claim-eligibility contradictions, missing hardware sync comparison rows, and forbidden payload dump fields.

No-payload for benchmark artifacts and verifier diagnostics means they may include metadata such as device paths, requested byte counts, iterations, concurrency, duration, backend, suite, mode, target, operation counts, rates, failure class, error kind, validation phase, direct failure kind, completion status, completion result, bytes completed, fault address, stdout/stderr paths, and launcher status. They must not include copied source bytes, destination bytes, raw payload dumps, or fields intended to serialize payload content.

## Downstream slice contract baselines

Each downstream M009 slice must preserve the baseline below before declaring a readability refactor complete.

| Slice | Baseline obligation | Refactor is allowed only if |
|---|---|---|
| S02 | Public `idxd-rust` API and contract-test baseline. | The public sync/async construction, validation report, accessor, and proof-binary contracts remain intentionally stable or are updated with explicit compatibility tests. |
| S03 | `idxd-rust` direct async readability baseline. | Completion ownership, request recovery, backpressure/retry metadata, and direct failure classification stay observable through host-free tests and verifier artifacts. |
| S04 | `idxd-rust` proof/verifier/artifact readability baseline. | Sync, async, and benchmark proof artifacts keep required fields, stdout/artifact equality, expected-failure classification, and no-payload redaction. |
| S05 | `idxd-sys` raw-boundary readability baseline. | Raw descriptor/completion layout, unsafe submission preconditions, status bytes, and OS error propagation stay transparent and testable without hardware. |
| S06 | `hw-eval` readability baseline. | CLI behavior, benchmark report schema, plotting consumers, software-only ordinary-host checks, and prepared-host claim boundaries remain stable. |

The S02-S06 references above are deliberately mechanical. The guard script rejects this report if any downstream slice reference disappears, because later agents need a single checklist before touching code.

## Non-change boundaries

M009 readability cleanup must not accidentally become a behavior rewrite. Unless a later slice explicitly versions the affected contract and updates every producer, verifier, and consumer together, these boundaries are non-changeable:

- No DSA/IAX hardware semantics rewrite, batching-policy rewrite, MOVDIR64/MOVDIR64B policy change, or fallback-to-software behavior change.
- No broad `idxd-sys` domain-error layer or builder layer that hides raw ABI/MMIO facts.
- No public async request allocation convenience API that weakens explicit source/destination ownership or recovery semantics.
- No proof-binary or benchmark JSON field rename/removal without an explicit replacement contract.
- No graphing/report schema drift in `hw-eval` without updating plotting consumers and tracked samples together.
- No use of ignored `.gsd/` or generated `target/` artifacts as verification fixtures.
- No copied source payload bytes, destination payload bytes, buffer dumps, or payload-content examples in diagnostics, reports, logs, manifests, or test fixtures.

S01 itself changes documentation and mechanical verification baselines only. Runtime behavior is intentionally unchanged.

## Verification matrix

The completed S01 baseline should map every readability move to a host-free proof first, then name prepared-host proof only for real hardware claims.

| Surface | Ordinary-host baseline | Prepared-host limit |
|---|---|---|
| `idxd-rust` public contracts | Contract tests and verifier artifact validation can prove field stability, classification stability, expected failures, and redaction without DSA/IAX hardware. | Hardware success and performance claims still require launch-wrapped prepared-host runs. |
| `idxd-rust` direct async runtime | Host-free tests can prove lifecycle, request recovery, backpressure, retry metadata, completion snapshot propagation, and no-payload formatting. | Real ENQCMD completion success requires a prepared device and launcher. |
| `idxd-sys` raw boundary | Layout tests and compile checks can preserve ABI shapes, raw helper behavior, and OS error propagation. | Real portal open/submission success requires prepared work queues and privileges. |
| `hw-eval` benchmark reports | Software-only or static report checks can preserve schema, plotting compatibility, and diagnostic classification. | DSA/IAX throughput, latency, NUMA, and accelerator-performance conclusions require prepared-host evidence. |
| Documentation guard | `bash idxd-rust/scripts/check_m009_s01_readability_baseline.sh` fails fast on missing sections, missing S02-S06 references, missing R018 coverage, and missing no-payload language. | Not applicable; the documentation guard is intentionally host-free. |

## R018 coverage

R018 is advanced by making the hardware Rust readability cleanup falsifiable before code motion begins. This scaffold gives maintainers a single baseline that names responsibilities, non-change boundaries, downstream slice obligations, host-free checks, and prepared-host limits.

Later S01 tasks must complete R018 coverage by filling concrete hotspots and contract references for `idxd-rust`, `idxd-sys`, and `hw-eval`. Downstream S02-S06 tasks then preserve or explicitly update those references while making the code easier to read.

## Ordinary-host and prepared-host limits

Ordinary-host verification can prove documentation completeness, compile-time API compatibility, host-free contract tests, verifier behavior, malformed-artifact rejection, expected-failure classification, schema stability, and no-payload redaction. It cannot prove that DSA/IAX hardware completed work successfully.

Prepared-host verification is required for any claim that a hardware-backed memmove, direct async submission, IAX/DSA benchmark path, or performance result succeeded. Prepared-host evidence must still preserve the same artifact contracts and no-payload constraints used on ordinary hosts.

No-payload means diagnostics may include metadata such as paths, requested byte counts, retry counts, phases, status bytes, completion results, fault addresses, benchmark names, concurrency, accelerator type, OS errors, and claim eligibility. Diagnostics must not include copied source bytes, destination bytes, raw payload dumps, or fields intended to serialize payload content.

## Reader-test result

A cold maintainer can use this baseline to see that S01 is documentation-only, that S02 has a named direct async seam map for owner/session, request recovery, direct registration/submission, pending operation lifecycle, monitor polling, completion/retry/recovery, and fake/backend seams, that S03 has a named Tokio benchmark map for CLI/config parsing, execution modes, software diagnostic proof, artifact serialization, output writing, verifier semantics, schema fields, claim eligibility, stdout/artifact equality, and no-payload constraints, that S02-S06 each have named contract baselines, that `idxd-rust`, `idxd-sys`, and `hw-eval` have distinct responsibilities, and that ordinary-host checks protect readability work without pretending to prove prepared-host hardware success.
