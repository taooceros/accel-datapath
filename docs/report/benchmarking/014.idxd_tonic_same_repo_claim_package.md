# IDXD + Tonic same-repo claim package

**Date**: 2026-04-24  
**Type**: canonical post-cleanup claim boundary / narrow conclusion

## Question

For the curated unary Tonic workloads in this repo, what does the surviving M001/M002 evidence boundary actually prove today about the ordinary-versus-IDXD comparison, and what would need to be rerun on a prepared host before anyone should make a stronger claim?

## Current claim in one paragraph

The trustworthy claim after cleanup is narrow. This repo retains three proof surfaces: the S02 software verifier, the S03 IDXD-aware verifier, and the optional S04 claim-package workflow that assembles their outputs into one comparison tree and report. Those surfaces prove that the repo can validate the software path, validate the IDXD path contract, and generate a stable comparison package with fixed summary/report locations. They do **not** currently prove a fresh prepared-host end-to-end same-host ordinary-versus-IDXD result on this machine, because the current `latest/` comparison remains mixed provenance when S03 preflight is blocked and the S04 workflow seeds the IDXD subtree from the frozen fixture.

## Stable evidence package

This report remains grounded in the manifest-declared S04 outputs:

- `accel-rpc/target/s04-claim-package/latest/summary/comparison_summary.json`
- `accel-rpc/target/s04-claim-package/latest/summary/ordinary_vs_idxd.csv`
- `accel-rpc/target/s04-claim-package/latest/summary/claim_table.md`

The manifest for the package is tracked at `accel-rpc/target/s04-claim-package/latest/manifest.json`, and it freezes three run families:

- `software_baseline`
- `software_attribution`
- `idxd_attribution`

That contract is the right thing to trust first. The prose should follow those surfaces, not older milestone ceremony.

## What each surviving proof surface proves

### S02: retained software verifier

The S02 path proves that the curated ordinary-path workloads can still be rerun and validated under the retained software-only workflow, with stable workload labels and endpoint artifacts that match the S04 manifest.

### S03: retained IDXD-aware verifier

The S03 path proves that the repo still has an explicit IDXD-aware validation boundary with visible preflight outcomes. When the host is properly prepared, this is the gate that refreshes the accelerated endpoint artifacts used by S04. When the host is not prepared, the failure remains observable instead of being hidden.

### S04: optional comparison and reporting workflow

The S04 path proves that the repo can assemble the software evidence, IDXD evidence, and control-floor reference into one stable claim-package tree with a machine-readable summary, CSV, markdown claim table, and this canonical report path. S04 is a comparison-and-report surface; it does not magically upgrade mixed-provenance inputs into a stronger scientific claim.

## Current host status and caveats

A live IDXD rerun is still blocked here when the launcher preflight is not satisfied. The retained S04 runner makes that explicit through `phase=`-labeled output and keeps the package inspectable by seeding `accel-rpc/target/s04-claim-package/latest/idxd/` from the frozen fixture artifacts when the live S03 path cannot refresh them.

That behavior is useful because it preserves a stable report tree and keeps drift visible, but it also means the current `latest/` comparison must be read as **mixed provenance** unless a prepared host reruns the accelerated subtree end to end.

## What the current comparison surfaces show

The current `latest/` summary outputs do **not** show an IDXD win for the curated workloads:

- bytes/client: IDXD is about `0.005x` the software baseline throughput
- bytes/server: IDXD is about `0.653x` the software baseline throughput
- proto/client: IDXD is about `0.003x` the software baseline throughput
- proto/server: IDXD is about `0.425x` the software baseline throughput

The stage totals are also not uniformly better on the accelerated side. Only proto/client shows a lower IDXD-attribution stage total than software attribution; the other rows are higher on the current IDXD side.

These observations matter, but they still need to be read through the caveat above: the package is currently useful as a narrow, inspectable comparison surface, not as a clean fresh same-host proof that the accelerated path wins.

## Narrow conclusion

The honest post-cleanup conclusion is:

1. The surviving repo entrypoints prove the **workflow boundary**: S02 software validation, S03 IDXD-path validation, and S04 comparison/report packaging are all still defined and inspectable.
2. The current `latest/` tables do **not** support a positive claim that the IDXD path clearly beats the ordinary path for these workloads.
3. Because the current package can be mixed provenance when S03 preflight is blocked, it also does **not** support a stronger claim about live prepared-host accelerator/runtime interaction than the retained surfaces actually show.

So the correct insider statement today is:

> Trust the retained verification and packaging boundary, not a broad acceleration story. The repo can still validate the software path, validate the IDXD path contract, and publish a stable ordinary-versus-IDXD claim package, but the present `latest/` outputs are only a narrow blocking or falsifying comparison surface until a prepared host refreshes the accelerated subtree from live hardware.

## What to trust now

Trust the following statements now:

- The canonical report path is `docs/report/benchmarking/014.idxd_tonic_same_repo_claim_package.md` and it still points at the stable generated summary outputs.
- The S02 verifier remains the trustworthy software-side proof surface for the curated workloads in scope.
- The S03 verifier remains the trustworthy IDXD-side contract check and prepared-host gate.
- The S04 workflow remains the trustworthy way to assemble one repo-local comparison package with stable `summary_path` and `report_path` outputs.
- The currently generated tables are sufficient to reject any casual claim that this repo already shows a clear IDXD win for the curated workloads.

## What to rerun next

For any stronger claim, rerun the retained validate/claim-package flow on a prepared host where the launcher preflight succeeds and `latest/idxd/` is refreshed from live hardware rather than fixture data.

At minimum, the next stronger claim should depend on:

1. a successful prepared-host S03 validation run,
2. a regenerated S04 claim package using that fresh accelerated subtree, and
3. a fresh review of the stable summary outputs above.

Until that happens, treat the current package as the honest narrow record of repo state, not as evidence that follow-on execution should automatically continue into M003.

## Planning note

M005 closes with a pause. This report may identify prepared-host reruns or future follow-on work, but it should not be read as authorization to auto-start M003 immediately after M005 finishes.
