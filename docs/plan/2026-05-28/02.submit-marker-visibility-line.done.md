# Submit-marker visibility line

## Goal
Refresh the Experiment 2 trace plot so it marks the first submit index where the sampled iteration's single completion-status read can view the marker completion.

## Scope
- Keep the benchmark setup unchanged: DSA noop, burst 160, marker first, poll offset 1, 1000 iterations, pinned core 0.
- Add only the sampled status byte already read by the latency measurement to per-iteration trace output.
- Regenerate the hardware JSON, CSV, and PNG plot.

## Verification
- Targeted Rust tests and release build.
- Hardware run via `launch` on `/dev/dsa/wq0.0`.
- Validate per-iteration traces and first visible index from JSON/CSV.

## Outcome
- Added visible-completion guide lines to the trace plot.
- Regenerated data and plot from a hardware rerun.
- Selected plotted iteration: 538; visible reads at submit indices 7..160.
