# Remove repo-local Turso KB tooling

## Goal

Remove the obsolete repo-local `.turso`/`tursodb` knowledge-base setup from the active checkout so the repository no longer advertises or imports the Turso KB workflow.

## Scope

- Delete the generated `.turso/` database directory from this checkout.
- Remove the tracked `tursodb/` wrapper/tooling directory and the `tursodb-kb` agent skill.
- Remove active configuration and guidance that imports or recommends the Turso KB path.
- Keep unrelated tools such as `codemogger` intact, even if they have their own Turso-named implementation dependencies.
- Preserve historical completed plan records unless they are active executable tooling.

## Intended changes

- Update root `devenv.yaml` to stop importing `./tursodb`.
- Update root `AGENTS.md` source-order guidance to remove Turso KB.
- Update docs/spec guidance that references local Turso rebuilds.
- Remove `.turso/`, `tursodb/`, and `.agents/skills/tursodb-kb/`.

## Verification

- Search for remaining active `.turso`, `tursodb`, and `Turso KB` references outside historical plan records and unrelated package lock dependencies.
- Inspect `git status --short` to confirm the intended removal set and avoid touching unrelated pre-existing changes.

## Completion notes

Removed the generated `.turso/` directory, the tracked `tursodb/` tooling tree, and the repo-local `tursodb-kb` skill. Updated active agent/devenv/docs/presentation references so the project no longer imports or recommends the Turso KB workflow. Historical completed plan records remain as project history.
