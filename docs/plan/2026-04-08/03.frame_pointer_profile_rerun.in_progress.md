# Frame-pointer tonic profiling rerun plan

**Date**: 2026-04-08
**Task**: rerun the representative tonic profiling captures after frame-pointer enablement and record both client and server results
**Source files**:
  - `.cargo/config.toml` — frame-pointer rustflags
  - `docs/plan/2026-04-01/09.tonic_only_profiling_experiment_plan.in_progress.md` — representative profiling procedure
  - `docs/report/benchmarking/009.tonic_profile_bounded_matrix_results.md` — prior representative points and artifact layout
  - `results/tonic/2026-04-01-loop2/` — previous client-side profiling outputs
**Output**: `results/tonic/2026-04-08-frameptr/`

---

## Goal

Repeat the representative profiling lane with frame pointers enabled so the perf/flamegraph captures have better unwinding quality, and add the missing server-side profiling captures alongside the client-side captures.

## Scope

Rerun the four representative points from report 009:

1. `small_struct_off_single` — `256 B`, `structured`, compression `off`, concurrency `1`, runtime `single`
2. `medium_struct_off_multi` — `4 KiB`, `structured`, compression `off`, concurrency `128`, runtime `multi`
3. `large_struct_off_multi` — `1 MiB`, `structured`, compression `off`, concurrency `512`, runtime `multi`
4. `large_struct_on_multi` — `64 KiB`, `structured`, compression `on`, concurrency `128`, runtime `multi`

For each point, collect profiling artifacts for **both** processes:

- client: perf stat, perf record + perf report, flamegraph
- server: perf stat, perf record + perf report, flamegraph

Keep the same placement assumptions as report 009 unless the harness forces a minor adjustment:

- server core `0`
- client core `1`
- loopback transport
- split `server` + `client` mode

## Preconditions

- verify frame pointers remain enabled via `.cargo/config.toml`
- rebuild `tonic-profile` in release mode before capturing profiles
- store all new artifacts under `results/tonic/2026-04-08-frameptr/`

## Execution notes

- use unique ports per representative point to avoid overlap
- run one profiling mode at a time per point so the server process can be profiled cleanly
- for server-side captures, start the profiled server first, then run the matching client workload, then terminate the server cleanly and materialize the profiling output
- preserve stderr/stdout sidecar files for perf/flamegraph invocations

## Planned artifact layout

Per point, expect filenames shaped like:

- `<name>_client_perf_stat.txt`
- `<name>_client_perf_stat.json`
- `<name>_client.perf.data`
- `<name>_client_perf_report.txt`
- `<name>_client_flamegraph.svg`
- `<name>_server_perf_stat.txt`
- `<name>_server_perf_stat.json`
- `<name>_server.perf.data`
- `<name>_server_perf_report.txt`
- `<name>_server_flamegraph.svg`

## Deliverables

1. a fresh result directory with client + server profiling artifacts for the four representative points
2. a short comparison summary against `results/tonic/2026-04-01-loop2/`
3. the matching canonical thread file updated first with the rerun status and artifact path, then the `current.md` dashboard entry refreshed
