# Goal

Revise `presentation/2026-04-08/tonic_flamegraph_analysis.typ` so the audience sees which tonic hotspots are strong hardware-offload candidates versus software-only cleanup, while preserving the existing deck style and traceability to report 009.

## Sources

- `docs/report/benchmarking/009.tonic_profile_bounded_matrix_results.md`
- `presentation/2026-04-08/tonic_flamegraph_analysis.typ`
- `presentation/template.typ`

## Intended output

- Updated 4-slide Typst deck at `presentation/2026-04-08/tonic_flamegraph_analysis.typ`
- Successful Typst compile with no syntax errors

## Design goals

- Keep the title slide metadata and overall palette/template styling intact
- Preserve the 4-regime table as the quantitative evidence slide
- Add an explicit offload-potential signal per regime using badge-style visuals
- Replace the current software-only hotspot slide with a clean offload-vs-software split
- End with a concise takeaway slide that reinforces DMA relevance and Prost irrelevance

## Story spine

1. Open with the same framing question: where time goes in tonic gRPC.
2. Show that the answer changes by regime, then add the missing interpretation layer: offload potential.
3. Separate hardware-helpful work from software-only work so the audience knows what DSA/IAX-class offload would actually target.
4. Close with the decision rule: medium/large copy paths are the right bottleneck; software buffer management is the prerequisite, not the alternative.

## Planned slide sequence

1. **Title** — unchanged framing and metadata.
2. **4 Regimes, 4 Different Bottlenecks** — existing table plus new `Offload Potential` column and retained legend.
3. **Where Hardware Offloading Helps — And Where It Doesn't** — two columns: offload targets vs software-only wins, with complementary note on buffer lifecycle work.
4. **Takeaway** — bold callout summarizing DMA fit, Prost non-issue, and software prerequisites.

## Slide-by-slide content components

### Slide 1 — Tonic gRPC: Where Does the Time Actually Go?
- Purpose: establish continuity with the existing deck
- Components: title, subtitle, perf metadata, report reference
- Visual guidance: leave unchanged unless spacing adjustments are needed for consistency

### Slide 2 — 4 Regimes, 4 Different Bottlenecks
- Purpose: present the evidence table and add the offloadability interpretation
- Key takeaway: offloadability depends on whether the CPU is stalled on movement versus actively executing useful work
- Components:
  - regime rows for small / medium / large / compression
  - columns for IPC, frontend, backend-mem, retiring, top hotspots, and new offload-potential badges
  - keep the original color legend for IPC/topdown severity
  - offload labels:
    - Small: `LOW`
    - Medium: `MEDIUM`
    - Large: `HIGH`
    - Compression: `HIGH*`
  - brief justification text in each offload cell

### Slide 3 — Where Hardware Offloading Helps — And Where It Doesn't
- Purpose: correct the framing issue directly
- Key takeaway: copy/compression are candidate engines for hardware, while lock/header/protobuf items remain software concerns
- Components:
  - left card/panel: offload targets
    - large payload `memmove` 71.37%
    - medium payload `memmove + malloc + growth` about 20%
    - compression `compress_inner` when a compression accelerator exists
  - right card/panel: software-only wins
    - H2 stream locks / futex contention
    - HPACK encode small contribution
    - Prost encode/decode negligible contribution
  - bottom callout: pooled buffers / pre-sizing are complementary enablers for offload because they expose larger, more stable transfer regions and reduce allocator churn

### Slide 4 — Takeaway
- Purpose: leave the audience with a crisp decision summary
- Key takeaway sentence: memmove dominates the medium/large uncompressed regimes, so DMA targets the right bottleneck; software buffer management prepares the path; protobuf is not worth hardware attention
- Components:
  - one prominent callout with the exact requested summary
  - optional supporting mini-cards or notes for three short bullets if needed for balance

## Visual guidance

- Stay within the existing palette from `presentation/template.typ`
- Use `card`, `panel`, `callout`, and `fit-badge` instead of inventing new primitives
- Make Slide 3 visually binary: left side more hardware-colored/green-blue, right side warmer/red-orange for software-only items
- Keep slide density moderate and presentation-readable on a 16:9 screen

## Delivery guidance

- Slide 2 should take the most time; it is the evidence slide
- Slide 3 should sound like a decision filter: “offload this, not that”
- Slide 4 should be a short closer, not a new analysis section

## Steps

1. Update the deck source with the new story structure and components. ✅
2. Compile the Typst file and fix any errors. ✅
3. Record completion in `current.md`. ✅
