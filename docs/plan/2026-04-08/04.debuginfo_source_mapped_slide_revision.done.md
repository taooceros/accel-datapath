# Goal

Revise `presentation/2026-04-08/tonic_flamegraph_analysis.typ` again using the debuginfo rerun so the audience can see not just caller-resolved hotspot buckets, but the concrete source files and lines where the dominant copy paths originate.

## Sources

- `docs/report/benchmarking/011.debug_symbol_rerun_client_server_results.md`
- `results/tonic/2026-04-08-frameptr-debuginfo/`
- `presentation/2026-04-08/tonic_flamegraph_analysis.typ`
- `presentation/template.typ`

## Intended output

- Updated `presentation/2026-04-08/tonic_flamegraph_analysis.typ` with source-mapped hotspot annotations
- Clean compile of the revised deck

## Design goals

- Preserve the frame-pointer revision story while enriching it with file/line evidence
- Keep the deck presentation-readable; source file mappings should clarify, not clutter
- Prefer a few high-value mappings over exhaustive code listings
- Make it obvious which hotspots are benchmark harness artifacts versus tonic/prost/library paths

## Planned content changes

1. Update provenance to cite the debuginfo rerun report.
2. Pull the best source-mapped examples from the large uncompressed client/server lane.
3. Add file/line annotations to the large-copy attribution slide for:
   - harness/request copy path in `accel-rpc/tonic-profile/src/main.rs`
   - `tonic-prost/src/codec.rs`
   - `tonic/src/codec/decode.rs`
   - any concrete `BytesMut`/buffer growth site if line mapping is available and not too noisy
4. Adjust takeaway wording so the slide explicitly distinguishes repo-owned sources from vendored tonic sources and libc leaf work.

## Steps

1. Inspect debuginfo rerun artifacts for usable file/line mappings. ✅
2. Revise the slide deck with source-backed annotations. ✅
3. Compile the deck and fix any issues. ✅
4. Mark plan complete and update `current.md`. ✅
