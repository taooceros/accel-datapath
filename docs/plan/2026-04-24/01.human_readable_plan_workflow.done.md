# Human-readable plan workflow

## Goal

Update the repo planning policy so plan documents are written for humans first, with enough context and narrative that a reviewer can understand the intended work, verification, and outcome without needing agent-only workflow knowledge.

## Scope

- update root `AGENTS.md` plan-writing guidance
- update `docs/AGENTS.md` to reinforce plan readability expectations inside the docs subtree
- commit only the workflow-policy files and this plan artifact

## Planned changes

1. Add explicit guidance in root `AGENTS.md` that plans must be human-readable and explain the change in plain language.
2. Add docs-specific guidance in `docs/AGENTS.md` that plan files are reviewable project records, not agent scratchpads.
3. Verify the updated guidance reads cleanly and stage only the related files.

## Verification

- Read back the edited guidance files and confirm they now require human-readable plans.
- Check `git diff --cached --stat` before committing to keep the change focused.

## Completion notes

- Root `AGENTS.md` now requires plans to explain goal, scope, intended changes, verification, and completion notes in plain language.
- `docs/AGENTS.md` now explicitly treats `docs/plan/` files as human-readable workflow records and forbids agent-only scratchpad style.
- Verified the updated text by reading both guidance files back after editing.
