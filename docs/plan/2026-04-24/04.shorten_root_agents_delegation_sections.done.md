# Shorten root AGENTS.md delegation sections

## Goal

Reduce the length of the root `AGENTS.md` by collapsing the delegation-related sections into a single clearer section while preserving the existing delegation rules.

## Scope

- edit root `AGENTS.md`
- merge `EXPLORER WORKFLOW` and `AGENT TEMPLATES` into one shorter section
- keep the same repo policy and references
- commit only the structural cleanup and this plan artifact

## Planned changes

1. Re-read the current delegation-related sections and identify the rules that need to survive the merge.
2. Replace the two adjacent sections with one compact section that still covers explorer usage, template location, naming, and citation expectations.
3. Read the final file back to confirm the structure is shorter and the guidance is still clear.

## Verification

- Read the updated `AGENTS.md` after editing.
- Review the staged diff before committing so the change stays focused.

## Completion notes

- Replaced the separate explorer and agent-template sections with a single `DELEGATION` section.
- Preserved the existing rules for explorer use, template location and naming, citation expectations, and restating budgets or stop conditions.
- Read the final `AGENTS.md` back after editing to confirm the file was shorter without losing delegation guidance.
