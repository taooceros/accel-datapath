# Remove obsolete parent-level `tursodb` directory

## Goal

Delete the older partial setup at `/home/hongtao/accel-datapath/tursodb` so only the repo-local `tursodb` environment remains.

## Plan

1. Remove the obsolete parent-level `tursodb` directory.
2. Keep the active setup under `/home/hongtao/accel-datapath/agent-env-wt/tursodb` unchanged.
3. Mark the cleanup plan complete.
