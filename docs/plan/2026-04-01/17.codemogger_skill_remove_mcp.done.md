# Add codemogger skill and remove direct MCP config

## Goal

Make `codemogger` available through a repo-local skill and stop exposing it through the direct `opencode` MCP configuration.

## Scope

- add `.agents/skills/codemogger/SKILL.md`
- document CLI-based indexing and search workflow for repo code discovery
- remove the `codemogger` MCP entry from `opencode.json`
- keep guidance aligned with existing `tursodb-kb` and root `AGENTS.md` retrieval split

## Result

- added `.agents/skills/codemogger/SKILL.md` for CLI-based code search workflow
- removed the direct `codemogger` MCP entry from `opencode.json`
- verified that `opencode.json` remains valid JSON after the config removal
