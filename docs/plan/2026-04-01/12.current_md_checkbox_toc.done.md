# Checkbox TOC for current.md

## Goal

Adjust the `current.md` TOC format so section links use Markdown task-list entries for quicker status scanning.

## Scope

- update root `AGENTS.md`
- update root `current.md`

## Planned changes

1. Define checkbox-style TOC entries in root `AGENTS.md`.
2. Update `current.md` so TOC items use `- [x]` / `- [ ]` markers.
3. Record the change in completed items once the update lands.

## Completion notes

- Root `AGENTS.md` now requires checkbox-style TOC entries in `current.md`.
- `current.md` TOC entries now use `- [x]` / `- [ ]` markers.
