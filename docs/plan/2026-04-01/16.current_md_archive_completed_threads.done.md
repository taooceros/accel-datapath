# Archive completed current.md threads

## Goal

Refine the `current.md` workflow so `current.md` keeps only active and paused threads, while fully completed threads are removed from the ledger and documented in workflow reports instead.

## Scope

- update root `AGENTS.md`
- update root `current.md`

## Planned changes

1. Require completed threads to be removed from `current.md` once their completion has been recorded in a durable artifact.
2. Keep `current.md` focused on active and paused threads only.
3. Restore the profiling thread as the active thread after the workflow-policy refinement is finished.

## Completion notes

- Root `AGENTS.md` now requires fully completed threads to be removed from `current.md` after their outcome is captured in a durable artifact.
- `current.md` has been reduced back to active and resume sections for the current profiling work.
- The finished workflow-policy thread was removed from `current.md` after its outcome had already been captured in `docs/report/workflow/003.current_md_run_ledger_policy.md` and the completed plan notes.
