# Experiment 2 no-black-box rerun plan

## Goal

Remove the `core::hint::black_box` wrapper around the completion-status value in Experiment 2 polling and rerun the same DSA `submit-marker-overlap` measurement to see whether the visible-read latency pattern changes.

## Scope

- Change only the status read handling in `hw-eval/src/benchmarks/submission_bottleneck/experiment_2_marker_overlap.rs`.
- Preserve benchmark selector, JSON schema, and plotting inputs.
- Build and run targeted checks before hardware measurement.
- Rerun Experiment 2 through `launch` using the same 2026-05-31 settings.
- Regenerate the latency-order plot from the new run.

## Verification

- `cargo test -p hw-eval experiment_2_marker_overlap`
- `cargo build --release -p hw-eval`
- Hardware run through `launch` emits parseable JSON.
- JSON contains one `submit_marker_overlap` row, 1000 per-iteration traces, and 160 points per trace.
- Plot script writes non-empty PNG and CSV artifacts.

## Completion notes

Completed and later folded into the mechanism/baseline trace work. The old duplicate no-`black_box` raw/JSON/plot artifacts and `032`/`033` reports were removed during cleanup; the surviving canonical results are the try2 overlap/mechanism trace artifacts and `docs/report/benchmarking/037.experiment2_mechanism_trace_try2_2026-05-31.md`.
