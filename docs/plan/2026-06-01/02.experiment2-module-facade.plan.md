# Experiment 2 module facade

## Goal

Keep the Experiment 2 outer Rust module as a small export facade now that the baseline path is encoded in the mechanism-probe suite.

## Scope

- Move the existing `submit-marker-overlap` implementation details out of `experiment_2_marker_overlap.rs` into a private child implementation module.
- Keep `submit-marker-overlap` and `submit-marker-mechanism` callable through the same outer module.
- Preserve CLI selectors and JSON/report result shapes.

## Intended changes

- Add a private child module for the overlap implementation.
- Make `experiment_2_marker_overlap.rs` declare private child modules and re-export only benchmark entry methods.
- Update the submission-bottleneck dispatcher to import both Experiment 2 methods from the outer module rather than reaching into child modules.

## Verification

- Run targeted `hw-eval` tests that cover the CLI contract and module compilation.
- Use software/no-hardware checks only; do not run hardware-facing binaries directly.

## Completion notes

Completed. The outer Experiment 2 module now declares private `overlap` and `mechanism_probes` modules and re-exports only `bench_submit_marker_overlap` and `bench_submit_marker_mechanism_probes`. The parent submission-bottleneck dispatcher imports both methods through that facade. Local experiment documentation was updated to describe the facade/private implementation split.

Verification performed:

- `cargo test -p hw-eval`
- `cargo fmt -p hw-eval --check`
