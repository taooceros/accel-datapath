# Deck style framework plan

## Goal

Refactor `presentation/2026-05-27/ai_coding_skill_formation_discussion.typ` so slide typography is controlled by a small shared presentation framework instead of one-off `#text(size: ...)` choices scattered through slide bodies.

## Scope

- Keep the current audience-facing story and page count unchanged.
- Preserve the recent page 8 structure: title/subtitle, retention-crash scoring panel, and bottom interpretation prompt.
- Replace ad hoc text-size literals in slide content with semantic helpers such as title, subtitle, metric, prompt, score, task label, arrow, and statement roles.
- Keep actual font sizes centralized near the top of the Typst file.

## Intended changes

1. Add a typography scale and semantic text helpers near the color definitions.
2. Update existing deck helper macros to use those helpers.
3. Replace slide-body custom text sizes with semantic helpers.
4. Regenerate PNG previews and compile the PDF to verify the Typst file and layout still render.

## Verification

- Compile PNG previews for visual inspection.
- Read the updated page 8 PNG because it is the currently contested slide.
- Compile the final PDF.
- Scan the Typst source to confirm no slide-body literal `text(size: ...pt)` remains outside the centralized framework.
