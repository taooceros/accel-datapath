# Deck coroutine directed-debugging revision plan

## Goal

Revise the coroutine alignment personal-trace slide so it explains the user's actual AI-use pattern: asking Claude to debug while giving it the right technical direction, rather than presenting the bug as purely solo debugging.

## Sources

- Current user request: describe how the user asked Claude to debug the coroutine memory-alignment problem while pointing Claude in the right direction.
- `docs/report/architecture/001.design_decisions.md`: DSA descriptors require 64-byte alignment, completion records require 32-byte alignment, and runtime over-alignment is used because coroutine-frame member `alignas()` is not reliable.
- `dsa-stdexec/src/dsa/dsa_operation_base.hpp`: implementation computes aligned descriptor/completion pointers once from over-allocated buffers and caches the pointers.

## Intended slide change

Revise slide 16 (`06 · PERSONAL TRACE`) to show learning-oriented delegation:

- Title: keep the coroutine alignment bug as the personal example.
- Top line: make the contrast explicit: `I asked Claude to debug, but I supplied the hypothesis and boundary.`
- Three panels:
  1. `MY DIRECTION`: focus on DSA descriptor/completion alignment across coroutine-frame storage.
  2. `CLAUDE'S WORK`: inspect the storage layout, check whether `alignas()` is enough, and trace where addresses are formed.
  3. `FIX`: over-allocate, compute aligned pointers once, and cache hot-path addresses.
- Bottom technical model: preserve the formula `(base + align - 1) & ~(align - 1)` and connect it to the hardware contract.
- Reflection band: `Learning-oriented delegation: outsource the search, keep hypothesis, evidence, and judgment in the human loop.`

## Verification

- Compile PNG previews.
- Inspect slide 16 for readability.
- Compile the PDF.
- Confirm no TODO placeholder appears in the deck source or notes.
