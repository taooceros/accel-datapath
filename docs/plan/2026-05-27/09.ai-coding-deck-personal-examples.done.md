# Deck personal examples plan

## Goal

Replace the `TODO` placeholder in `presentation/2026-05-27/ai_coding_skill_formation_discussion.typ` with personal examples from this repo's agent-history workflow.

## Source grounding

- Current OMP session transcript: `/home/hongtao/.omp/agent/sessions/-accel-datapath-async-binding-intel-idxd/2026-05-27T04-56-15-412Z_019e67ca-5b34-7000-81b2-45283eb1abd7.jsonl`.
- Deck edit history and execution plans under `agents/plan/2026-05-27/`.
- `hw-eval` modularity cleanup execution plan as a repo example of an agent satisfying a maintainability/verification request.

## Intended slide content

Add two open-floor slides before references:

1. A concrete trace from this deck-editing session showing where the assistant failed the user's intent and where user supervision corrected it.
   - Unsatisfying examples: missing the title/subtitle hierarchy issue, making the page-8 scoring layout worse, using ad hoc text sizes, and manually filled page numbers.
   - Satisfying examples: page-4 pipeline balancing, the user-sketched retention-crash panel, the centralized typography framework, and Touying auto counters.
2. A repo workflow takeaway slide connecting the personal trace to the paper's broader point.
   - Effective agent use looked like active review: concrete artifacts, explicit invariants, visual checks, and verification.
   - Risky agent use looked like delegation: accepting generated changes without enough inspection.

## Verification

- Compile PNG previews.
- Inspect the new personal-example slides.
- Compile the PDF.
- Confirm no `TODO` placeholder remains in the Typst source.
