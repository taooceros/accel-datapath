# Deck page-number automation plan

## Goal

Replace manually filled slide numbers in `presentation/2026-05-27/ai_coding_skill_formation_discussion.typ` with Touying's automatic slide counter.

## Scope

- Keep the current visual design and slide content unchanged.
- Use Touying's `utils.slide-counter.display()` and `utils.last-slide-number` for the top-right slide number.
- Remove the manual `n` argument from the shared `story-slide` helper and every call site.
- Preserve the 16:9 page size, margins, and paper fill under Touying's slide wrapper.

## Verification

- Compile PNG previews.
- Inspect page 8 preview to verify the auto number renders in the header.
- Compile the final PDF.
- Scan source for remaining manual `N / M` slide-number literals.
