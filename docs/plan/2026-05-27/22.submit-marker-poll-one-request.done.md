# Submit marker poll one request

## Goal
Experiment 2 should poll only one completion record per submit-index observation. This isolates the marker request and removes the four-completion polling overhead from the active observation loop.

## Intended change
The benchmark will keep one trace point per submit index, but each polled trace point will inspect only the marker completion record. The result schema stays compatible: the `completions` list will contain one completion trace and `tracked_completions` will be 1.

## Verification
Use a release hardware rerun to confirm trace length remains N and the result records exactly one tracked completion.

## Completion notes
The benchmark now isolates marker observation by polling one request per traced submit index. The result schema remains compatible, with `tracked_completions = 1` and a one-element `completions` list. The offset 1/32/64 hardware rerun completed without missing/error completions and generated updated plots.
