# DSA submission deck topic-module refactor plan

## Goal

Organize the DSA submission bottleneck deck by narrative topic rather than by individual slide files.

## Scope

- Preserve the primary entry file `presentation/2026-05-26/dsa_submission_bottleneck_experiments/dsa_submission_bottleneck_experiments.typ`.
- Keep the 17 existing slides and their current order.
- Group slide functions into topic files: introduction, experiment 1 through experiment 5, and attribution.
- Remove the one-slide-per-file module layout from the previous refactor.

## Intended changes

- Add topic modules under `topics/`.
- Move the existing slide bodies into named functions in those topic modules.
- Update the entry file to import topics and call the topic functions in presentation order.
- Delete the now-obsolete `slides/` modules that were introduced by the previous split.

## Verification

- Compile the deck with `typst compile --root presentation` to a temporary output PDF.
- Programmatically check topic module headings, entry imports, slide calls, and page breaks.

## Completion notes

Completed. The deck is organized by topic rather than by individual slide: introduction, experiment 1 submit occupancy, experiment 2 marker overlap, experiment 3 traffic ladder, experiment 4 completion reuse, experiment 5 blind push, and attribution. The primary entry file imports those topic modules and preserves the 17-slide sequence. The obsolete one-slide-per-file `slides/` directory was removed. Verification passed with a Typst compile to `/tmp/dsa_submission_bottleneck_experiments.topic-modules.pdf` and a programmatic module/order check.

## Tinymist formatting pass

Completed after the topic-module refactor. Tinymist LSP reported `documentFormattingProvider=true`; formatting was requested for the entry file, `support.typ`, and all topic modules. Six topic modules changed, and a post-format Typst compile plus module-shape check passed.

## Touying slide splitting

Updated after review: the entry file no longer calls `#pagebreak()` between topic slide functions. Touying handles the slide boundaries from the level-1 slide headings inside the topic modules. Verification compiled `/tmp/dsa_submission_bottleneck_experiments.touying-split.pdf` successfully and `pdfinfo` reported 17 pages; the module-shape check reports zero entry-file pagebreaks.
