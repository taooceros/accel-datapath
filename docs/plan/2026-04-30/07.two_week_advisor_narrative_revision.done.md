# Two-week advisor narrative revision

## Goal

Revise the two-week progress deck so it reads like a human advisor-meeting narrative rather than a status ledger. The deck should connect the current Rust IDXD work to the earlier `dsa-stdexec` experiment method and make one small, easy-to-follow claim.

## Context read

- `presentation/2026-03-31/progress_2026-03-31.typ` for the previous two-month project narrative.
- `presentation/2026-04-08/tonic_research_story.typ` for the DSA-to-Tonic research arc.
- `docs/report/benchmarking/006.stdexec_overhead_results.md` for the layer-removal experiment results.
- `dsa-stdexec/benchmark/dsa/README.md` and `dsa-stdexec/benchmark/dsa/strategies/README.md` for the experiment framework and strategy taxonomy.
- Current reports for Tonic claim boundaries, direct Tokio async IDXD, generic IDXD proof, and representative measured rows.

## Planned changes

1. Start the deck with the advisor-facing story: earlier DSA experiments showed that batching makes software overhead visible; the last two weeks built a cleaner Rust IDXD path to test the same idea in Tonic.
2. Add a slide that explicitly maps the `dsa-stdexec` experiment method to the next Tonic experiment: baseline, remove/control software cost, add hardware path, compare matched configurations.
3. Keep a few concrete numbers from the earlier and current work, but avoid letting numbers dominate the deck.
4. Preserve the small claim: the Rust IDXD path is cleaner and has a small hardware proof; there is no Tonic speedup claim yet.
5. Recompile the PDF and verify the revised narrative text appears.

## Verification

- `typst compile presentation/2026-04-30/two_week_progress_2026-04-30.typ presentation/2026-04-30/two_week_progress_2026-04-30.pdf --root .`
- `pdftotext` checks for the old DSA lesson, the reused experiment method, the small claim, current measured rows, and next Tonic rerun.
- Confirm the deck remains a reasonable page count.

## Completion notes

- Reworked the deck into an advisor-facing narrative instead of a status ledger.
- Added the earlier `dsa-stdexec` lesson: compare controlled layers, remove software overhead step by step, then check whether the result transfers to real hardware.
- Mapped that method to the next Tonic experiment: ordinary Tonic, software control Tonic, then IDXD Tonic on matched workloads.
- Kept concrete details from prior and current work, including `26.3 Mpps`, `59.9 Mpps`, `48.4 Mops/s`, and the current `146,246 ops/s` / `459,064 ops/s` representative rows.
- Recompiled the PDF and verified the narrative headings, key numbers, and removed-jargon check.
