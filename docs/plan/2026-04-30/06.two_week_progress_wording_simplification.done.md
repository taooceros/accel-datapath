# Two-week progress presentation wording simplification

## Goal

Revise the existing two-week progress deck so the wording is simpler, the claim is smaller, and the story is easier to follow in a project meeting.

## Scope

- Update only the presentation wording and generated PDF.
- Keep the same evidence base and measured numbers.
- Avoid broad or unusual terms such as "falsification surface" or "proof seam".
- Preserve the distinction between what is proved now and what still needs a prepared-host Tonic rerun.

## Planned changes

1. Reframe the deck around one small claim: the Rust IDXD path is cleaner and has a limited real-hardware proof; it is not yet a Tonic speedup claim.
2. Simplify slide headings and bullet text.
3. Recompile the PDF and verify the revised text appears in the generated deck.

## Verification

- Compile the Typst deck with `typst compile ... --root .`.
- Use `pdftotext` to confirm the simplified claim and key sections are present.
- Confirm the PDF has the expected page count.

## Completion notes

- Rewrote the deck around one small claim: the Rust IDXD path is cleaner and has a limited real-hardware proof, but this is not yet a Tonic speedup result.
- Replaced jargon-heavy wording with simpler slide titles and shorter bullets.
- Recompiled the PDF and verified the revised text plus page count.
