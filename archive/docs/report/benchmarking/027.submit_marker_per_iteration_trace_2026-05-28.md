# Submit-marker per-iteration trace rerun, 2026-05-28

## Question

After changing Experiment 2 to emit one `sample_trace` entry per hardware iteration, does the rerun contain the expected per-iteration trace data, and can the plot be drawn from one randomly selected concrete iteration?

## Hardware run

```bash
launch ./target/release/hw-eval \
  --accel dsa \
  --benchmark submit-marker-overlap \
  --device /dev/dsa/wq0.0 \
  --json \
  --iterations 1000 \
  --marker-bursts 160 \
  --marker-positions first \
  --marker-poll-offsets 1 \
  --dsa-op noop \
  --pin-core 0
```

## Artifacts

- Full raw/JSON trace files were generated but are not committed here because they are large generated artifacts.
- `docs/report/benchmarking/submission_bottleneck_2026-05-27/submit_poll_trace_per_iteration_offset1_noop.png`
- `docs/report/benchmarking/submission_bottleneck_2026-05-27/submit_poll_trace_per_iteration_offset1_noop.csv`

## Validation snapshot

- Result rows: 1.
- Iterations: 1000.
- Burst length `n`: 160.
- `sample_trace` entries: 1000.
- Points per selected sample trace: 160.
- Completion medians: completed 160, missing 0, errors 0.
- Aggregate submit-tail median: 60508 TSC ticks.
- Aggregate marker-visible median: 60776 TSC ticks.

The refreshed plot uses iteration 538 for the rendered trace and draws a vertical guide at every submit index where the sampled status read observed the marker completion.

Selected iteration 538:

- visible completion submit indices: 7..160, 154 points.
- submit latency: min 24, median 26, max 34 TSC ticks.
- single completion-status read latency: min 24, median 26, max 572 TSC ticks.

Across all 1000 sampled iterations, the first visible marker completion appeared between submit indices 2 and 9, with median first-visible index 7.

## Verification

- `cargo fmt -p hw-eval` succeeded.
- `cargo test -p hw-eval experiment_2_marker_overlap` passed.
- `cargo build --release -p hw-eval` succeeded.
- `python -m py_compile docs/report/benchmarking/submission_bottleneck_2026-05-27/plot_submit_poll_trace.py` succeeded.
- Hardware run through `launch` succeeded.
- JSON validation passed: one result row, 1000 per-iteration traces, 160 points per trace, completion medians 160/0/0.
- Visibility validation passed: all 1000 sampled iterations observed the marker completion during traced polls; first-visible min/median/max = 2/7/9.
- Plot generation succeeded and produced non-empty PNG/CSV artifacts with visible-completion guide lines.
