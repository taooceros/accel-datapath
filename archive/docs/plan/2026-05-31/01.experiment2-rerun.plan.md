# Experiment 2 rerun and plot plan

## Goal

Rerun the DSA submission-bottleneck Experiment 2 (`submit-marker-overlap`) on the configured DSA work queue and regenerate the per-iteration submit/poll trace plot from the new result.

## Scope

- Build the release `hw-eval` binary.
- Run the hardware benchmark through `launch` using the documented DSA WQ flow.
- Store the raw launcher output, parsed JSON, plot PNG, and plot CSV under a dated benchmarking artifact directory.
- Validate that the benchmark emitted the expected Experiment 2 row and per-iteration trace data.

## Intended command shape

Use `--benchmark submit-marker-overlap`, `/dev/dsa/wq0.0`, `--iterations 1000`, `--marker-bursts 160`, `--marker-positions first`, `--marker-poll-offsets 1`, `--dsa-op noop`, and `--pin-core 0`, matching the latest Experiment 2 trace runs.

## Verification

- `cargo build --release -p hw-eval`
- Hardware run through `launch` succeeds and emits parseable JSON.
- JSON contains one `submit_marker_overlap` row with non-empty `sample_trace` and complete 160-point traces.
- Plot script writes non-empty PNG and CSV artifacts.

## Completion notes

Completed and later superseded by the canonical try2 trace artifacts under `docs/report/benchmarking/submission_bottleneck_2026-05-31/`. The earlier duplicate raw/JSON/plot artifacts and `031` report were removed during cleanup.
