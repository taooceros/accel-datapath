# Experiment 2 all-variant trace run plan

## Goal

Run the focused Experiment 2 baseline and every Experiment 2 mechanism-probe variant, then generate one trace plot and CSV per result row from the same selected iteration so the rows are visually comparable.

## Scope

- Use the established focused setup: DSA noop, burst 160, poll offset 1, 1000 iterations, pinned core 0, `/dev/dsa/wq0.0`.
- Rerun `submit-marker-overlap` for the baseline trace row.
- Rerun `submit-marker-mechanism`, which executes the mechanism variants sequentially: baseline, padded layout, prefetch distances, batch-scan timing, pre-touch, and clflush.
- Generate per-row PNG and CSV trace artifacts under `docs/report/benchmarking/submission_bottleneck_2026-05-31/`.
- Record validation and interpretation in `docs/report/benchmarking/`.

## Verification

- `cargo build --release -p hw-eval` succeeds.
- Hardware runs go through `launch` and emit parseable JSON with non-empty per-iteration `sample_trace` data.
- Every expected mechanism row is present and has 1000 traces with 160 points each.
- Plot generation succeeds for baseline plus every mechanism variant.

## Completion notes

Completed. The release binary was built, the focused overlap baseline and all mechanism variants were rerun through `launch`, and one CSV/PNG trace pair was generated for each row using random sample iteration 804. Validation confirmed 9 rows, 1000 traces per row, 160 points per selected trace, and clean completion medians. Details are in `docs/report/benchmarking/038.experiment2_all_variant_traces_2026-05-31.md`.
