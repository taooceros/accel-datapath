# Submission-bottleneck experiment doc split plan

## Goal

Split the submission-bottleneck experiment notes into one local markdown document per experiment under `hw-eval/src/benchmarks/submission_bottleneck/`.

## Scope

- Keep a short `EXPERIMENTS.md` index in the folder.
- Add one markdown file for each experiment, matching the existing module numbering.
- Move the detailed Experiment 2 rerun/cacheline notes into the Experiment 2 document.
- Keep other experiment documents concise and grounded in their current module headers.

## Verification

- Re-read the generated files for obvious structure mistakes.
- Run `git diff --check` on the affected markdown files.

## Completion notes

Completed. `EXPERIMENTS.md` is now a short index, and each submission-bottleneck experiment has its own markdown document beside the implementation module. The detailed Experiment 2 latency/cacheline notes moved into `experiment_2_marker_overlap.md`. `git diff --check` passed for the affected markdown files.
