# DSA submission deck marker-position illustration plan

## Goal

Make Experiment 2 easier to understand by adding a visual explanation of marker position inside a submit burst.

## Scope

- Target `presentation/2026-05-26/dsa_submission_bottleneck_experiments/`.
- Add one Experiment 2 explanation slide near the existing Experiment 2 setup.
- Show `p=first`, `p=N/2`, and `p=N` as a descriptor sequence with one marker descriptor and no-completion fillers.
- Explain the timing predicate `tc < t1` in terms of the marker completing while CPU submission continues after the marker.
- Preserve existing result slides and Lilaq plots.

## Verification

- Compile the deck to PDF.
- Regenerate PNG previews.
- Inspect the new marker-position preview slide.
- Check rendered text/page order.

## Completion notes

Completed. Added an Experiment 2 marker-position illustration slide immediately after the Experiment 2 setup slide. The slide shows `p = first`, `p = N/2`, and `p = last` as marker placement inside the CPU submit order, explains `tc < t1`, and states why earlier markers leave more submit tail for overlap. Verification compiled the deck, regenerated previews, inspected the new preview slide, and checked rendered text/page order.
