# DSA submission deck interleaved-results visualization plan

## Goal

Move the appended experiment result slides next to their corresponding experiment sections and add visual plots so each result is easier to read from the slide itself.

## Scope

- Target `presentation/2026-05-26/dsa_submission_bottleneck_experiments/`.
- Keep the topic-module layout.
- Reorder the entry file so each experiment's result slide immediately follows that experiment's setup/motivation/control slides.
- Add generated SVG plot assets under `assets/plots/` for Experiments 1--5.
- Update result slides to show the plot as the primary evidence, while keeping compact numeric anchors and source lines.
- Preserve the final measured-answer synthesis slide after the attribution rule.

## Verification

- Compile the final PDF with `typst compile --root presentation`.
- Regenerate PNG previews under `assets/previews/`.
- Inspect the modified preview slides visually.
- Check rendered PDF page count, result-slide order, and key plot asset references.

## Completion notes

Completed. Result slides now appear immediately after their matching experiment sections, and Experiments 1--5 use Lilaq (`@preview/lilaq:0.6.0`) plots as the primary visualization. Verification compiled the deck, regenerated previews, visually inspected the plotted result slides, and checked rendered text/page order.
