# DSA submission deck result-append plan

## Goal

Append the measured 2026-05-27 experiment results to the DSA submission bottleneck presentation so the deck no longer stops at the Experiment 1 result.

## Scope

- Target `presentation/2026-05-26/dsa_submission_bottleneck_experiments/`.
- Keep the existing topic-module layout and append result slides after the current Experiment 1 result.
- Ground all numbers in `docs/report/benchmarking/020.submission_bottleneck_experiments_2026-05-27.md`; keep the existing Experiment 1 result grounded in report 019.
- Do not change the experiment setup/why-run-it slides except where the entry metadata needs a source/claim-boundary update.

## Intended slide changes

- Add an Experiment 2 result slide: early marker overlap is visible during the submit tail when polling is enabled; half-position overlap appears clearly at `N=256`; last-position markers do not overlap.
- Add an Experiment 3 result slide: submit-only, NOOP+completion, memmove64, and memmove4k all show the submit-admission knee in the same window-120 to window-128 region; 64 B payload does not move the knee.
- Add an Experiment 4 result slide: completion reuse policies remain in roughly the same throughput band and do not explain the 116--117 outstanding-operation admission knee.
- Add an Experiment 5 result slide: over-depth distinct-descriptor admission through burst 1024 completed every descriptor with zero missing completions and zero descriptor errors.
- Add a concise final result synthesis slide if needed to make the appended evidence answer the deck’s attribution question.

## Verification

- Compile the deck with `typst compile --root presentation` to a temporary PDF.
- Compile PNG previews under `assets/previews/` for visual-layout inspection.
- Extract PDF text and check that the appended result slides and key report numbers are present.
- Check PDF page count and heading order after the append.

## Completion notes

Completed. The presentation now appends measured result slides for Experiments 2--5 plus a final measured-answer synthesis slide after the existing Experiment 1 result. The appended slides are grounded in `docs/report/benchmarking/020.submission_bottleneck_experiments_2026-05-27.md` and preserve the existing topic-module layout. Verification compiled the final PDF, regenerated PNG previews under `assets/previews/`, inspected the appended preview slides, and checked the rendered PDF text/page count.
