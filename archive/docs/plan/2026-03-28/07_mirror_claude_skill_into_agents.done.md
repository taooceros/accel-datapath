# Mirror repo-local skill into .agents

## Goal

Create a repo-local `.agents` tree that mirrors the existing `.claude` skill so OpenCode-style tooling has the same project-scoped content.

## Scope

- create `.agents/skills/literature-review/`
- copy the repo-local `SKILL.md` content from `.claude`
- keep the mirror exact so the two repo-local skill entrypoints stay aligned

## Result

- added `.agents/skills/literature-review/SKILL.md`
- mirrored the current `.claude` literature-review skill content exactly
