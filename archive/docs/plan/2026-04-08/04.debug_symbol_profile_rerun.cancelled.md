# Debug-symbol tonic profiling rerun plan

**Date**: 2026-04-08
**Task**: rerun the representative tonic profiling captures with release debuginfo enabled, while keeping frame pointers and recording both client and server results
**Source files**:
  - `.cargo/config.toml` — frame-pointer rustflags
  - `docs/plan/2026-04-01/09.tonic_only_profiling_experiment_plan.in_progress.md` — representative profiling procedure
  - `docs/plan/2026-04-08/03.frame_pointer_profile_rerun.cancelled.md` — prior frame-pointer rerun procedure
  - `docs/report/benchmarking/010.frame_pointer_rerun_client_server_results.md` — immediate baseline without release debuginfo
**Output**: `results/tonic/2026-04-08-frameptr-debuginfo/`

**Status**: cancelled on 2026-04-15

Historical note:
Relationship: Preserved as a historical planning record; future work should start from a fresh plan instead of reviving this stale in-progress file.


---

## Goal

Repeat the same four representative profiling points with release debuginfo enabled so the perf reports and flamegraphs have richer symbolization, while preserving the frame-pointer setting and the client/server artifact symmetry from the previous rerun.

## Scope

Rerun these representative points:

1. `small_struct_off_single` — `256 B`, `structured`, compression `off`, concurrency `1`, runtime `single`
2. `medium_struct_off_multi` — `4 KiB`, `structured`, compression `off`, concurrency `128`, runtime `multi`
3. `large_struct_off_multi` — `1 MiB`, `structured`, compression `off`, concurrency `512`, runtime `multi`
4. `large_struct_on_multi` — `64 KiB`, `structured`, compression `on`, concurrency `128`, runtime `multi`

For each point, collect:

- client: perf stat, perf record + perf report, flamegraph
- server: perf stat, perf record + perf report, flamegraph

## Build/config approach

- keep frame pointers from `.cargo/config.toml`
- enable release debuginfo at build time via `CARGO_PROFILE_RELEASE_DEBUG=true`
- avoid persisting a repo-wide Cargo profile change unless the rerun shows that permanent config is needed later

## Execution notes

- use a fresh output directory: `results/tonic/2026-04-08-frameptr-debuginfo/`
- verify the rebuilt release binary contains debug sections before profiling
- use the same core placement as the prior rerun: server core `0`, client core `1`
- preserve stdout/stderr sidecars for perf/flamegraph commands
- keep server-side profiling flow identical to the prior rerun: start profiled server, run profiled client workload, then terminate the server cleanly

## Deliverables

1. debuginfo-enabled client/server profiling artifacts for the four representative points
2. a short comparison against `results/tonic/2026-04-08-frameptr/`
3. a report describing whether release debuginfo materially improved profile readability/attribution
