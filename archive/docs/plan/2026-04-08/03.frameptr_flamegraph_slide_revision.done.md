# Goal

Revise `presentation/2026-04-08/tonic_flamegraph_analysis.typ` so it reflects the 2026-04-08 frame-pointer rerun and no longer presents large-message `memmove` as a prost-ambiguous monolith. The revised deck should show which large-message copy costs land in client/server prost encode, prost decode, tonic body buffering, and harness/request construction.

## Sources

- `docs/report/benchmarking/009.tonic_profile_bounded_matrix_results.md`
- `docs/report/benchmarking/010.frame_pointer_rerun_client_server_results.md`
- `results/tonic/2026-04-08-frameptr/*_perf_report.txt`
- `results/tonic/2026-04-08-frameptr/*_perf_stat.txt`
- `presentation/2026-04-08/tonic_flamegraph_analysis.typ`
- `presentation/template.typ`

## Intended output

- Updated `presentation/2026-04-08/tonic_flamegraph_analysis.typ`
- Clean Typst compile of the revised deck

## Design goals

- Preserve the existing deck style, title framing, and quantitative regime table where still valid
- Correct all statements that implied prost encode/decode were globally negligible
- Add one audience-facing slide that explains the new caller-resolved attribution in the large uncompressed regime
- Keep the offload story, but make it precise: the target is the whole copy pipeline, not prost alone
- Keep claims traceable to the new client/server frame-pointer artifacts

## Story spine

1. Keep the original regime-level story: small is control-path dominated, medium is allocator/copy heavy, large uncompressed is movement dominated, compressed is DEFLATE dominated.
2. Add the missing refinement from the rerun: large-message copy time is split across multiple concrete callers.
3. Show that client and server both spend substantial time in prost encode/decode and tonic body buffering, while the client also pays harness-level clone/request-construction cost.
4. Close with the corrected optimization message: offload the copy pipeline and fix harness/buffer discipline first; do not claim “prost is irrelevant” without scoping it.

## Planned slide sequence

1. **Title** — unchanged except for data provenance line mentioning frame-pointer rerun when useful.
2. **Repo Question / stack context** — keep, but soften any line claiming Prost is negligible everywhere.
3. **4 Regimes, 4 Different Bottlenecks** — keep table, but update the large uncompressed hotspot text to say the `memmove` monolith is now caller-resolved.
4. **Large 1 MiB Uncompressed: Where the Copy Time Lands** — new evidence slide with client/server split and exact caller percentages.
5. **Offload framing** — revise offload-vs-software split so it targets the copy pipeline instead of dismissing prost outright.
6. **Takeaway / architecture validation** — keep structure but revise the protobuf claim to client/server-scoped wording.

## Slide-by-slide content components

### Slide 1 — Tonic gRPC: Where Does the Time Actually Go?
- Purpose: maintain continuity
- Components: title, subtitle, perf metadata, report references
- Update: include `010.frame_pointer_rerun_client_server_results` in provenance or note that representative points were rerun with frame pointers and server-side profiles

### Slide 1b — The Repo Question: Where Can Hardware Offloading Help?
- Purpose: map the tonic stack and crate split to the profiling story
- Update components:
  - revise `Prost encode / decode` stage-card text from “negligible” to “large bytes fields become bulk copy callers in frame-pointer rerun”
  - revise the codec/buffer layer text to emphasize shared responsibility with prost/body buffering

### Slide 2 — 4 Regimes, 4 Different Bottlenecks
- Purpose: preserve the regime table
- Update components:
  - leave small/medium/compression rows substantially unchanged
  - in the large uncompressed row, replace the old single-line `memmove 71.37%` interpretation with text like “copy pipeline resolved into prost encode/decode + body buffering + harness clone on rerun”
  - keep offload-potential badge as `HIGH`, but justify it as whole-pipeline movement offload

### Slide 3 — Large 1 MiB Uncompressed: Where the Copy Time Lands
- Purpose: central correction slide
- Key takeaway: frame pointers show the client/server copy pipeline, not a single prost-free `memmove` blob
- Components:
  - two side-by-side panels: client vs server
  - client bullets/cards with exact percentages:
    - `ProstEncoder::encode -> memmove` `22.62%`
    - `ProstDecoder::decode -> bytes::merge -> memmove` `16.22%`
    - `StreamingInner::poll_frame -> memmove` `10.06%`
    - `run_phase` worker / request-construction copy `18.01%` (identified as harness-side copy bucket)
    - `BytesMut reserve/realloc -> memmove` about `1.33%`
  - server bullets/cards with exact percentages:
    - `ProstDecoder::decode -> bytes::merge -> memmove` `22.75%`
    - `StreamingInner::poll_frame -> memmove` `17.08%`
    - `ProstEncoder::encode -> memmove` `13.37%`
    - lock contention / drop overhead as secondary context
  - bottom callout: “large uncompressed is still the best DMA target, but the target is the copy pipeline across encode/decode/body/harness — not ‘prost only’ and not ‘prost irrelevant’”

### Slide 4 — Where Hardware Offloading Helps — And Where It Doesn’t
- Purpose: keep the offload decision framing
- Update components:
  - offload targets should name “client+server copy pipeline” instead of only `memmove`
  - software-only wins should keep H2 locks / HPACK
  - protobuf card should be rewritten to say protobuf logic is simple, but its bytes-field encode/decode still participates in the copy pipeline for large messages

### Slide 5 — Takeaway / Architecture validation
- Purpose: corrected summary
- Update components:
  - avoid “Prost encode/decode is negligible at all sizes”
  - replace with “small-message protobuf logic is negligible; large `bytes` messages surface as copy callers, so the real target remains movement and buffer lifecycle”
  - architecture validation should emphasize `accel-codec` / buffer interception and the need to remove harness artifacts before making stronger claims

## Visual guidance

- Reuse `panel`, `card`, `callout`, and `fit-badge`
- Make the new attribution slide the densest quantitative slide after the regime table
- Use one color family for client-side buckets and another for server-side buckets, but keep the palette aligned with the current deck
- Prefer short percent-labelled bullets/cards over another wide table

## Delivery guidance

- Slide 3 is the corrective slide: explain it as “frame pointers told us whose copy time this is”
- Slide 4 should then sound less absolute: offload the movement path, not protobuf semantics
- End by explicitly separating artifact truth from future aspiration: current harness still needs phase timers / variant runs for cleaner exclusivity

## Steps

1. Update `current.md` for the slide revision. ✅
2. Revise the Typst deck to incorporate frame-pointer client/server attribution. ✅
3. Compile the revised deck and fix any syntax/layout errors. ✅
4. Mark plan complete and update `current.md` with the revised artifact state. ✅
