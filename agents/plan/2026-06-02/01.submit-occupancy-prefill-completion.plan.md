# Submit occupancy prefill completion execution plan

## Goal

Implement Experiment 1 prefill completion timing with minimal schema and hot-path disturbance.

## Scope

- Touch only the Experiment 1 runner, report structs, local experiment docs, and targeted tests if needed.
- Do not change benchmark selectors, occupancy defaults, or hardware launch behavior.

## Steps

1. Inspect existing Experiment 1 data flow and JSON contract tests.
2. Add a per-iteration prefill completion trace struct and populate it from a separate prefill-only probe after the measured iteration.
3. Update local Experiment 1 documentation for the new field semantics.
4. Verify with formatting and targeted tests.

## Verification

Use targeted `cargo test -p hw-eval` invocations for affected test cases, plus formatter check or formatting for changed Rust files.

## Completion notes

Implemented. The runner records prefill-completion samples with a companion prefill-only probe after the measured submit path, preserving the occupancy submit timing while measuring how long K requests take to finish. Verified with `cargo fmt -p hw-eval` and `cargo test -p hw-eval`.
