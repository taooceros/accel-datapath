# Submit occupancy spin overhead execution plan

## Goal

Implement the requested Experiment 1 manual spin overhead knob cleanly and verify the affected CLI/config/report path.

## Scope

- Work in `hw-eval` only, plus the required project and agent plan records.
- Keep the default behavior identical with spin iterations set to zero.
- Avoid hardware execution; use parser/serialization/contract tests and release check as verification.

## Steps

1. Add config field and Clap option for `--submit-occupancy-spin-iters`.
2. Pass the value through the submission-bottleneck dispatcher into Experiment 1.
3. Add a lean non-optimizable CPU-loop helper and apply it between submit calls while keeping measured submit timing scoped to `wq.submit` only.
4. Add result metadata and update JSON serialization test.
5. Update README/experiment docs and targeted tests.
6. Run focused Rust tests/checks for `hw-eval`.

## Verification

Use `rtk cargo test -p hw-eval ...` for changed unit/contract tests and `rtk cargo check -p hw-eval` for compilation coverage.

## Completion notes

The code path now carries `submit_occupancy_spin_iters` from Clap through `BenchmarkConfig`, the submission-bottleneck dispatcher, and Experiment 1. Experiment 1 applies a `black_box(n)` gap between submitted descriptors and labels human output plus JSON rows with the configured spin count. Documentation and focused tests were updated.
