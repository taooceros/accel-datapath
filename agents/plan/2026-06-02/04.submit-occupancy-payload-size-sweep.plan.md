# Submit occupancy payload-size sweep execution plan

## Goal

Run the requested Experiment 1 payload-size sweep with 10 iterations and enough memory control to include 1 GiB memmove descriptors.

## Scope

- Work in `hw-eval` Experiment 1 config/allocation path only if required for feasibility.
- Preserve defaults; use shared-payload allocation only when explicitly requested.
- Use `launch` for all hardware runs.

## Steps

1. Add shared-payload config plumbing and metadata if needed.
2. Verify cargo fmt/check/tests/build.
3. Run cap-320 TSC-gap sweeps for noop, 1 MiB, 10 MiB, 100 MiB, and 1 GiB with 10 iterations.
4. Analyze extra-submit knees and prefill/completion signals.
5. Write a concise benchmarking report.

## Verification

Targeted tests plus release build before hardware; raw JSON and summary CSV after hardware.

## Completion notes

Shared-payload allocation and incomplete-drain reporting are implemented. Hardware runs used `launch` and wrote raw per-K JSON under `hw-eval/results/2026-06-02-payload-size-sweep-iter10-perk/`; summary CSV and report are under `docs/report/benchmarking/046.submit_occupancy_payload_size_sweep_iter10.*`.

Follow-up sweep added the explicit `hardware_observed` trace field plus 4 KiB and 64 KiB payloads. Raw JSON is under `hw-eval/results/2026-06-02-payload-size-sweep-observed-iter10-perk/`; summary/report are `docs/report/benchmarking/049.submit_occupancy_payload_observed_sweep_iter10.*`.
