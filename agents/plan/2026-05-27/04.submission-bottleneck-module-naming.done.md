# Agent plan: rename submission bottleneck modules

## Goal

Apply the requested naming cleanup: the bottleneck implementation module is named `submission_bottleneck`, each experiment file is named `experiment_N_name.rs`, and the parent module uses the modern Rust `submission_bottleneck.rs` layout instead of `mod.rs`.

## Target files

- `hw-eval/src/methodology/`
- `hw-eval/README.md`
- current plan files documenting the refactor

## Constraints

- Rename only; do not alter experiment logic, CLI selectors, JSON fields, benchmark names, or hardware behavior.
- Keep one shared `common.rs` for non-experiment helpers.
- Verify with formatting, targeted CLI tests, and release build.

## Acceptance

The old bottleneck module path and directory `mod.rs` are gone, the new names compile, and README points to the new layout.

## Completion notes

Completed on 2026-05-27.

Implemented layout:

```text
hw-eval/src/methodology/submission_bottleneck.rs
hw-eval/src/methodology/submission_bottleneck/common.rs
hw-eval/src/methodology/submission_bottleneck/experiment_1_submit_occupancy.rs
hw-eval/src/methodology/submission_bottleneck/experiment_2_marker_overlap.rs
hw-eval/src/methodology/submission_bottleneck/experiment_3_traffic_ladder.rs
hw-eval/src/methodology/submission_bottleneck/experiment_4_completion_reuse.rs
hw-eval/src/methodology/submission_bottleneck/experiment_5_blind_push_correctness.rs
```

Experiment 5 is represented by moving the existing `submit-admission` correctness gate into `experiment_5_blind_push_correctness.rs`; no duplicate selector was added.

Verification performed:

- `cargo fmt -p hw-eval`
- `cargo test -p hw-eval --test cli_contract remaining_bottleneck`
- `cargo test -p hw-eval --test cli_contract missing_hardware_device_is_structured_nonzero_error`
- `cargo build --release -p hw-eval`
- `cargo test -p hw-eval config::tests`
