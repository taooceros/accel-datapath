# Agent plan: interleave DSA result slides and add plots

## Reader

Coding agents maintaining the DSA submission bottleneck presentation.

## Goal

Place measured results next to each experiment and add plot assets to the result slides.

## Scope

- Edit the deck entry file to interleave `experiment_N.result()` calls with their experiment sections.
- Generate static SVG charts in `assets/plots/` from the reported benchmark values.
- Edit each experiment result slide to use the matching plot image and keep concise interpretation text.
- Do not change the setup/why-run-it slide content unless required for ordering or source metadata.

## Verification

- Typst compile the deck to PDF.
- Generate PNG previews.
- Check page count, heading order, and presence of plot references.

## Completion notes

Completed. Result slides now appear immediately after their matching experiment sections, and Experiments 1--5 use Lilaq (`@preview/lilaq:0.6.0`) plots as the primary visualization. Verification compiled the deck, regenerated previews, visually inspected the plotted result slides, and checked rendered text/page order.
