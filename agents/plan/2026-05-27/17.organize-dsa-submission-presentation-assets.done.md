# Agent plan: organize DSA submission presentation assets

## Reader

Coding agents maintaining the Typst presentation tree.

## Goal

Move the `presentation/2026-05-26/dsa_submission_bottleneck_experiments.typ` deck and its generated artifacts into a dedicated deck-local folder while keeping generated preview images in a nested preview directory.

## Scope

- Create a dedicated folder under `presentation/2026-05-26/` for the DSA submission bottleneck deck.
- Move the `.typ` source and matching `.pdf` output into that folder.
- Move PNG preview/snapshot artifacts from the dated presentation directory into a nested preview/assets folder below the dedicated folder.
- Update relative Typst imports after the move.
- Verify the moved deck still compiles.

## Verification

- `typst compile` succeeds for the moved deck.
- The old top-level dated folder no longer contains the DSA deck source, PDF, or PNG preview artifacts.
- The dedicated folder contains the source/PDF and nested preview assets.

## Completion notes

- Moved the DSA submission bottleneck Typst source and PDF into `presentation/2026-05-26/dsa_submission_bottleneck_experiments/`.
- Moved all top-level PNG preview/snapshot artifacts from `presentation/2026-05-26/` into `presentation/2026-05-26/dsa_submission_bottleneck_experiments/assets/previews/`.
- Updated the deck import to `../../template.typ`.
