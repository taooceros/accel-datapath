# Agent plan: add ASCII experiment headers

## Goal

Add a concise ASCII illustration at the top of each DSA bottleneck experiment module so readers can immediately see what the experiment is testing.

## Target files

- `hw-eval/src/methodology/submission_bottleneck/experiment_1_submit_occupancy.rs`
- `hw-eval/src/methodology/submission_bottleneck/experiment_2_marker_overlap.rs`
- `hw-eval/src/methodology/submission_bottleneck/experiment_3_traffic_ladder.rs`
- `hw-eval/src/methodology/submission_bottleneck/experiment_4_completion_reuse.rs`

## Constraints

- Comments only; no CLI, JSON, or benchmark behavior changes.
- Keep the drawings compact and directly tied to the experiment semantics.
- Preserve terminology: occupancy/window are logical operations, one operation per MMIO submission, not DSA hardware batch descriptors.

## Verification

Run `cargo fmt -p hw-eval` after the comment-only edit to verify the Rust sources remain parseable and formatted.

## Completion notes

Added ASCII purpose diagrams to all four DSA bottleneck experiment modules and ran `cargo fmt -p hw-eval`.
