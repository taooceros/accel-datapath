# Agent plan: modularize DSA bottleneck experiment code

## Reader

Coding agents continuing the DSA bottleneck campaign.

## Goal

Refactor `hw-eval/src/methodology/submission_bottleneck.rs` into a module directory with one module per experiment, while preserving CLI, JSON, and runner behavior.

## Target files

- `hw-eval/src/methodology/submission_bottleneck.rs` becomes `hw-eval/src/methodology/submission_bottleneck/mod.rs` plus child modules.
- `hw-eval/README.md` structure section should mention the module directory.
- No config/report/main/schema changes are expected unless paths/imports require cleanup.

## Constraints

- Do not alter benchmark names, CLI selectors, JSON field names, or run semantics.
- Keep shared helper code in `common.rs`; do not duplicate descriptor preparation or completion counting across experiment modules.
- Keep each experiment module focused on exactly one experiment.
- Do not run hardware benchmarks for this refactor unless compiler fixes force behavior-sensitive changes.

## Acceptance

Completed.

The flat bottleneck file is gone. The new directory contains one module per experiment plus `common.rs` for shared descriptor/completion helpers. Existing public runner names are re-exported from `mod.rs`, so DSA dispatch remains stable.

Verification performed by caller:

- `cargo fmt -p hw-eval`
- `cargo test -p hw-eval config::tests`
- `cargo test -p hw-eval --test cli_contract remaining_bottleneck`
- `cargo test -p hw-eval --test cli_contract submit_occupancy`
- `cargo build --release -p hw-eval`
