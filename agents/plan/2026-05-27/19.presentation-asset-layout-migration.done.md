# Agent plan: migrate presentation assets to deck-local layout

## Reader

Coding agents coordinating presentation file organization.

## Goal

Use multiple task agents to move existing presentation decks into dedicated folders and nest preview PNGs under each deck's `assets/previews/` directory, then verify and commit the layout migration.

## Scope

- Delegate date clusters to non-overlapping task agents.
- Each task agent investigates its assigned directories before moving files.
- Update relative Typst imports after moving each deck one level deeper.
- Verify moved decks compile.
- Commit only the presentation layout migration and its plan/policy artifacts.

## Verification

- Each task agent reports moved sources, preview folders, commands, and any special sidecars.
- Main agent performs a repository-level layout scan and targeted Typst compile check after all tasks finish.
- Main agent stages and commits only relevant files.

## Completion notes

Completed. Existing presentation decks were moved into deck-stem folders; generated PNG previews were nested under deck-local `assets/previews/` where present; moved sources were updated for the new relative template path where needed. `presentation/template.typ` now exports the legacy `slide-title` and `zebra-fill` helpers needed by the 2026-03-03 deck. The repository-level layout scan found zero dated-root `.typ`/`.pdf`/`.png` violations, and targeted Typst compiles passed for every migrated deck.
