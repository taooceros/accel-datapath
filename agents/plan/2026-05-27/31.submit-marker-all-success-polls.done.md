# Submit marker all success polls

## Goal
Record every successful marker poll in Experiment 2, not only the first successful observation per benchmark iteration.

## Scope
- Add per-trace-point success-only cumulative poll-end TSC stats.
- Add row-level stats/count for all successful marker polls observed during active polling.
- Keep the lean hot loop: submit, status load, post-load TSC, then accounting.
- Rerun offset-1 hardware data and summarize the success trajectory.

## Verification
Run targeted tests, release build, hardware rerun, and JSON checks for success-only fields.

## Completion notes
- Replaced the marker-only first-seen path with per-request completion observation accumulators.
- Hardware rerun for offset 1 recorded 160 request completion rows; every request was observed successfully in all 1000 iterations.
- Marker-visible compatibility fields now derive from request 1 observation state.
