# Agent plan: measured call helper

## Reader

Coding agents maintaining the `hw-eval` submission-bottleneck tracing code.

## Goal

Introduce a small measured-call helper that wraps a closure with TSC reads and returns both the closure result and the measured timestamps, then use it in Experiment 2's traced submit/poll path.

## Scope

- Add a narrow helper in the submission-bottleneck shared code.
- Refactor Experiment 2 traced submit and poll measurement to use the helper.
- Preserve the current JSON schema and measured behavior.

## Verification

Run formatting, targeted config/CLI tests, and a release build for `hw-eval`.

## Completion notes

Completed. Added `measure_call` / `MeasuredCall<T>` in the shared submission-bottleneck helper module and used it for the Experiment 2 traced submit and completion-status poll paths. Formatting, targeted tests, and release build passed.
