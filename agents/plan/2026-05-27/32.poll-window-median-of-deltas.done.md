# Poll window median of deltas

## Goal
Fix Experiment 2 poll-window reporting so the plotted value is median(poll_end - submit_end) computed per iteration, not median(poll_end) - median(submit_end).

## Scope
- Add per-trace-point poll-window TSC stats to the JSON result.
- Record the per-iteration poll window in the hot loop when polling is performed.
- Update the plotting script to use the emitted poll-window stats directly.
- Rerun the offset-1 hardware experiment and regenerate the plot.

## Verification
Run targeted unit tests, release build, hardware rerun, JSON checks, and plot generation.

## Completion notes
- Added `poll_window_tsc_ticks` and `poll_window_ns` to each Experiment 2 trace point.
- The benchmark now records `poll_end_tsc - submit_end_tsc` per iteration before computing stats.
- Updated the trace plotting script to use the emitted median and p99 poll-window stats directly.
- Reran offset-1 hardware data and regenerated request-completion plus submit/poll trace plots.
