# Agent plan: AI coding deck style framework

## Goal

Make the Typst deck use a centralized typography framework rather than per-slide custom text sizes.

## Scope

Target file: `presentation/2026-05-27/ai_coding_skill_formation_discussion.typ`.

Non-goals: changing the deck story, slide count, source claims, or presenter notes.

## Steps

1. Inspect all remaining `size:` usage in the Typst source.
2. Add named typography sizes and semantic helper macros at the top of the file.
3. Replace slide-content `#text(size: ...)` calls with helper macros.
4. Compile PNG previews, inspect page 8, compile the final PDF, and scan for remaining ad hoc text-size literals.

## Acceptance

- Page 8 keeps the user's preferred scoring-panel direction.
- Slide content no longer carries arbitrary text-size literals; sizes are expressed through framework roles.
- Typst PNG and PDF compilation succeeds.
