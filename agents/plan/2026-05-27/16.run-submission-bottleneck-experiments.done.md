# Agent plan: run submission bottleneck experiments

## Reader

Coding agents continuing the DSA submission-bottleneck benchmark campaign.

## Goal

Run the implemented submission-bottleneck experiment set on the same dedicated DSA WQ setup and present measured results with durable artifacts.

## Scope

- Use the release `hw-eval` binary through `launch`.
- Reconfirm the dedicated WQ metadata and the no-loss admission baseline.
- Run the explicit submission-bottleneck selectors needed to answer the campaign questions after the prior submit-occupancy report.
- Store raw JSON/raw stdout artifacts under `docs/report/benchmarking/` and write a concise benchmark report.

## Verification

- `cargo build --release -p hw-eval` succeeds before hardware runs.
- Hardware commands use `launch ./target/release/hw-eval` with `/dev/dsa/wq0.0`, `--json`, and `--pin-core 0`.
- Result parsing checks completion/missing/error fields before conclusions are reported.

## Completion notes

Completed. Ran the release `hw-eval` binary through `launch` for submit-only, submit-admission, marker-overlap, traffic-ladder, and completion-reuse-policy selectors. Stored raw and parsed artifacts under `docs/report/benchmarking/submission_bottleneck_2026-05-27/` and wrote `docs/report/benchmarking/020.submission_bottleneck_experiments_2026-05-27.md`.

During traffic-ladder validation, the memmove rows exposed unfaulted payload pages in the shared operation slots. Fixed `hw-eval/src/benchmarks/submission_bottleneck/common.rs` to pre-touch source/destination pages, rebuilt release, and reran traffic-ladder with zero missing/error medians across all traffic classes/windows.
