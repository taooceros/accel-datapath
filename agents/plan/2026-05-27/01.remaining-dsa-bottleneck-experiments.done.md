# Agent plan: implement remaining DSA bottleneck experiments

## Reader

Coding agents continuing the DSA bottleneck campaign in `hw-eval`.

## Goal

Implement the remaining experiment runners from `docs/plan/2026-05-26/02.dsa-submission-bottleneck-experiment.plan.md` without burying hardware logic in the old large DSA benchmark file.

## Delegation shape

A single implementation subagent owns the shared code cutover to avoid conflicting edits in `config.rs`, `report.rs`, `main.rs`, and `dsa.rs`.

Primary target files:

- `hw-eval/src/methodology/submission_bottleneck.rs`
- `hw-eval/src/config.rs`
- `hw-eval/src/report.rs`
- `hw-eval/src/main.rs`
- `hw-eval/src/methodology/dsa.rs`

Caller-owned follow-up files after implementation returns:

- `hw-eval/tests/cli_contract.rs`
- `hw-eval/README.md`
- relevant reports under `docs/report/benchmarking/`
- presentation plan/deck only after measured results exist

## Constraints

- Read `agents/CODING_REQUIREMENTS.md` and `hw-eval/AGENTS.md` before editing code.
- Keep one logical operation per MMIO submission; do not introduce DSA hardware `BATCH` descriptors for these experiments.
- Do not add duplicate hardware ABI/layout definitions.
- Do not run formatters, project-wide tests, release builds, or hardware-facing commands from subagents. The caller performs verification once across the combined edits.
- Preserve existing CLI and JSON behavior.
- Prefer small explicit structs/enums over broad framework abstractions.

## Acceptance

Completed.

Subagent `0-RestBottleneckExperiments` implemented the shared code cutover in:

- `hw-eval/src/methodology/submission_bottleneck.rs`
- `hw-eval/src/config.rs`
- `hw-eval/src/report.rs`
- `hw-eval/src/main.rs`
- `hw-eval/src/methodology/dsa.rs`

Caller follow-up added targeted config/CLI tests and README documentation, then ran formatter, targeted tests, release build, and one launcher smoke per new selector. No intentionally unsupported sub-variant remains in the default selector set.
