# Agent plan: update presentation preview layout policy

## Reader

Coding agents generating or editing Typst presentation previews.

## Goal

Update `presentation/AGENTS.md` so future preview PNGs are always generated into a deck-local nested preview asset folder.

## Scope

- Modify only `presentation/AGENTS.md`.
- Keep the rule concise and concrete, with an example command that writes previews under `assets/previews/` below the deck folder.
- Mark this plan done after the rule is updated and reviewed.

## Verification

- Re-read the modified convention lines and confirm the example path uses `assets/previews/` rather than the dated presentation root.

## Completion notes

Completed. `presentation/AGENTS.md` now requires generated preview PNGs to be written under each deck's `assets/previews/` folder and includes a concrete Typst compile example for the nested layout.
