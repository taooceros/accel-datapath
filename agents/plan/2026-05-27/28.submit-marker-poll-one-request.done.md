# Submit marker poll one request

## Goal
Change Experiment 2 so each traced poll samples only the marker completion record instead of four completion records. Keep submit latency recorded for every submit index.

## Scope
- Reduce the tracked completion count for `submit-marker-overlap` to one.
- Preserve the existing JSON shape and `poll_performed` indicator.
- Rerun the offset-1-focused hardware sample and plot submit/poll latency.

## Verification
Run targeted tests, release build, and hardware rerun confirming `tracked_completions = 1` and full trace length.

## Completion notes
- Experiment 2 now polls one completion record (`tracked_completions = 1`) per polled submit index.
- Submit latency remains recorded for every index 1..N.
- Hardware rerun for offsets 1, 32, and 64 produced trace length 160 for each row and one completion trace per point.
