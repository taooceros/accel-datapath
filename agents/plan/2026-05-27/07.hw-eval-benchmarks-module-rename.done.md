# hw-eval benchmark module rename execution plan

## Goal
Rename the internal benchmark implementation tree from `methodology` to `benchmarks` while preserving behavior.

## Steps completed
1. Moved the module root and directory to `benchmarks`.
2. Updated Rust imports/declarations from `methodology` to `benchmarks`, using sibling-relative imports inside the benchmark tree where clearer.
3. Updated README and `hw-eval/AGENTS.md` structure text.
4. Formatted and ran targeted verification.

## Constraints observed
- All CLI selectors and JSON keys were preserved.
- Unrelated worktree changes were left untouched.
- No hardware benchmark runs were performed because this is a module-path rename.

## Verification
- `cargo fmt -p hw-eval`
- `cargo test -p hw-eval config::tests` → 13 passed.
- `cargo test -p hw-eval --test cli_contract remaining_bottleneck` → 2 passed.
- `cargo test -p hw-eval --test cli_contract submit_occupancy` → 2 passed.
- `cargo fmt -p hw-eval -- --check`
- `cargo build --release -p hw-eval` → passed.
- `git diff --check`

## Completion notes
Completed.
