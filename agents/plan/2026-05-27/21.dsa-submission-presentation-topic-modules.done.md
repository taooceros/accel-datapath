# Agent plan: regroup DSA submission deck into topic modules

## Reader

Coding agents maintaining the Typst presentation source.

## Goal

Replace the one-module-per-slide split with topic-level Typst modules for the DSA submission bottleneck deck while preserving the same rendered slide sequence.

## Scope

- Target `presentation/2026-05-26/dsa_submission_bottleneck_experiments/`.
- Keep `support.typ` as shared deck-local helper code.
- Replace `slides/NN...typ` with topic modules such as introduction, per-experiment modules, and attribution.
- Preserve all 17 slide bodies, titles, ordering, package usage, and deck-level settings.

## Verification

- Compile the primary entry file with `typst compile --root presentation` to a temporary PDF.
- Check that topic modules cover the 17 headings in the expected order and the entry file no longer imports per-slide modules.

## Completion notes

Completed. The deck now has seven topic modules under `presentation/2026-05-26/dsa_submission_bottleneck_experiments/topics/`: `introduction.typ`, one module for each of experiments 1--5, and `attribution.typ`. Each topic module exports named slide functions, and the entry file calls them in the original 17-slide order. The previous one-slide-per-file `slides/` directory was removed. Verification passed with `typst compile --root presentation presentation/2026-05-26/dsa_submission_bottleneck_experiments/dsa_submission_bottleneck_experiments.typ /tmp/dsa_submission_bottleneck_experiments.topic-modules.pdf` and a module-shape check confirming seven topic modules, 17 exported slide functions, 16 page breaks, no `slides/` directory, and the original first/last slide headings.

## Tinymist formatting pass

Completed after the topic-module refactor. Tinymist LSP reported `documentFormattingProvider=true`; formatting was requested for the entry file, `support.typ`, and all topic modules. Six topic modules changed, and a post-format Typst compile plus module-shape check passed.

## Touying slide splitting

Updated after review: the entry file no longer calls `#pagebreak()` between topic slide functions. Touying handles the slide boundaries from the level-1 slide headings inside the topic modules. Verification compiled `/tmp/dsa_submission_bottleneck_experiments.touying-split.pdf` successfully and `pdfinfo` reported 17 pages; the module-shape check reports zero entry-file pagebreaks.
