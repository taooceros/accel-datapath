# Submit marker single sample trace

## Goal
Expose one concrete Experiment 2 iteration trace so plots can show an actual run instead of per-index medians across iterations.

## Scope
- Add a compact per-submit sample trace to the JSON result for one iteration.
- Capture exact submit latency, poll window, poll count/range, poll latencies, and visible prefix for that iteration.
- Update the submit/poll plotting script to prefer the sample trace when present.
- Rerun offset-1 hardware data and regenerate plots.

## Verification
Run targeted tests, release build, hardware rerun, JSON checks for sample trace length and fields, then plot generation.

## Completion notes
- Added `sample_trace` to Experiment 2 JSON with one concrete midpoint iteration.
- Each sample point records exact submit latency, poll window, polled request indices, per-status-read poll latencies, poll range, visible prefix, and marker status.
- Updated submit/poll plotting to use `sample_trace` rather than aggregate medians.
- Reran offset-1 hardware data and regenerated the one-trace plot/CSV.
