# Agent plan: Touying auto slide numbers

## Goal

Move slide numbers in the AI coding skill deck from manual literals to Touying automatic counters.

## Target

`presentation/2026-05-27/ai_coding_skill_formation_discussion.typ`

## Steps

1. Import Touying and make the existing deck pages run through `touying-slides` while preserving page dimensions, fill, and margins.
2. Add a `slide-number` helper that uses `utils.slide-counter.display()` and `utils.last-slide-number`.
3. Remove the manual page-number parameter from `header` / `story-slide` and all call sites.
4. Compile PNG previews and PDF, inspect page 8, and scan for remaining manual slide-number literals.

## Acceptance

- No `N / M` slide-number literals remain in `story-slide` calls.
- Page 8 header shows an automatically generated counter.
- PNG and PDF compilation succeed.
