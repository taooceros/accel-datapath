# Agent plan: add Experiment 2 marker-position illustration

## Reader

Coding agents maintaining the DSA submission bottleneck Typst deck.

## Goal

Add a clear visual slide explaining what marker position means in Experiment 2.

## Scope

- Edit `topics/experiment_2_marker_overlap.typ` to add a `marker_position()` slide function.
- Edit the entry file to call it immediately after `experiment_2.setup()`.
- Keep the slide compact and visual: one sentinel marker descriptor inside three burst-layout rows.
- Do not change benchmark numbers or result interpretation.

## Verification

- Typst compile final PDF.
- Regenerate previews.
- Verify new heading/order and visually inspect the new slide.

## Completion notes

Completed. Added an Experiment 2 marker-position illustration slide immediately after the Experiment 2 setup slide. The slide shows `p = first`, `p = N/2`, and `p = last` as marker placement inside the CPU submit order, explains `tc < t1`, and states why earlier markers leave more submit tail for overlap. Verification compiled the deck, regenerated previews, inspected the new preview slide, and checked rendered text/page order.
