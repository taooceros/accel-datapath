# hw-eval modularity cleanup execution plan

## Goal
Use delegated implementation passes to clean up `hw-eval` modularity without changing benchmark behavior.

## Delegation executed
1. `ModuleNamingLayout` converted the methodology root to the newer Rust layout, renamed Experiment 5 to `experiment_5_submit_admission_probe`, and refreshed README discovery text.
2. `CompletionReuseSplit` extracted named policy helpers from the Experiment 4 completion-reuse runner.
3. `BottleneckPlumbing` grouped submission-bottleneck config/results and moved dispatch behind `submission_bottleneck::run`.
4. The main agent integrated the edits, moved operation sizing semantics out of `config.rs`, formatted, and ran targeted verification.

## Constraints observed
- Subagents did not run project-wide tests, builds, formatters, benchmarks, or hardware commands.
- Existing selectors and JSON keys were preserved.
- Unrelated worktree changes were left untouched.
- Bottleneck experiment terminology remains one logical operation per MMIO.

## Verification
- `cargo fmt -p hw-eval`
- `cargo test -p hw-eval config::tests` → 13 passed.
- `cargo test -p hw-eval --test cli_contract remaining_bottleneck` → 2 passed.
- `cargo test -p hw-eval --test cli_contract missing_hardware_device_is_structured_nonzero_error` → 1 passed.
- `cargo fmt -p hw-eval -- --check`
- `cargo build --release -p hw-eval` → passed.
- `cargo test -p hw-eval --test cli_contract submit_occupancy` → 2 passed.
- `cargo test -p hw-eval --test cli_contract software_only_json_preserves_top_level_report_contract` → 1 passed.

## Completion notes
Completed.
