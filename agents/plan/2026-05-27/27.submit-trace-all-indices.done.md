# Submit trace all indices

## Goal
Record submit latency for every descriptor index in Experiment 2, independent of the configured polling offset. Polling should still begin only at the configured offset.

## Scope
- Update `submit-marker-overlap` trace allocation and hot loop so submit timing covers indices 1..N.
- Preserve completion polling fields only where polling actually runs.
- Keep JSON compatibility for existing `trace` shape.
- Verify with targeted hw-eval checks and a hardware rerun.

## Completion notes
- Experiment 2 now records submit timing for every index 1..N.
- Polling still starts at `marker_poll_offset`; each trace point includes `poll_performed`.
- Verified with targeted unit/config/CLI checks, release build, and hardware rerun showing trace length 160 for offsets 1, 32, and 64.
