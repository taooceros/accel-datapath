# Agent plan: submit-marker traced latency

## Reader

Coding agents continuing the DSA submission-bottleneck experiment-2 rewrite.

## Goal

Implement the simplified traced active submit loop requested by the user: fixed poll step 1, configurable poll offset, and serialized latency lists for plotting submit cost and poll behavior across the admission-wall region.

## Intended edits

- `hw-eval/src/config.rs`: add poll-offset parsing/configuration while keeping existing selector plumbing stable.
- `hw-eval/src/report.rs`: add serializable trace-point structs and fields under `submit_marker_overlap` rows.
- `hw-eval/src/benchmarks/submission_bottleneck/experiment_2_marker_overlap.rs`: replace cadence-overlap measurement with per-submit traced measurement from offset onward.
- `hw-eval/README.md` and targeted tests: document and validate the new option/schema.

## Constraints

- Follow `agents/CODING_REQUIREMENTS.md`.
- Preserve one logical operation per MMIO submission.
- Avoid duplicate hardware ABI definitions.
- Keep the output plot-friendly without emitting payload bytes.

## Verification

Run the narrow formatting, tests, release build, and a launch smoke if hardware is available.

## Completion notes

Completed. Rewrote `submit-marker-overlap` into a fixed-step traced poll-offset measurement, added plot-friendly trace JSON, ran targeted tests, built release, ran the hardware trace on `/dev/dsa/wq0.0`, wrote report 021, and updated the DSA bottleneck deck.
