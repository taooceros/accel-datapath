# Agent plan: append DSA submission experiment results to deck

## Reader

Coding agents maintaining the DSA submission bottleneck Typst deck.

## Goal

Append measured results from `docs/report/benchmarking/020.submission_bottleneck_experiments_2026-05-27.md` to the existing topic-module presentation.

## Scope

- Work in `presentation/2026-05-26/dsa_submission_bottleneck_experiments/`.
- Keep topic modules grouped by experiment.
- Add result functions to the relevant experiment topic modules and call them from the entry file after `#experiment_1.result()`.
- Update entry-file source metadata to include report 020 and the new result-append plan.
- Keep result slides compact and source-traceable; do not invent chart data beyond the report tables.

## Verification

- Typst compile the primary entry file with `--root presentation`.
- Generate PNG previews under `assets/previews/`.
- Use PDF text extraction plus a page/heading check to confirm the appended results render in order.

## Completion notes

Completed. The presentation now appends measured result slides for Experiments 2--5 plus a final measured-answer synthesis slide after the existing Experiment 1 result. The appended slides are grounded in `docs/report/benchmarking/020.submission_bottleneck_experiments_2026-05-27.md` and preserve the existing topic-module layout. Verification compiled the final PDF, regenerated PNG previews under `assets/previews/`, inspected the appended preview slides, and checked the rendered PDF text/page count.
