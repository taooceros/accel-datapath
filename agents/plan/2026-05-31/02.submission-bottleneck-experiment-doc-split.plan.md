# Submission-bottleneck experiment doc split agent plan

## Task

Split `hw-eval/src/benchmarks/submission_bottleneck/EXPERIMENTS.md` into one markdown document per experiment.

## Steps

1. Create `experiment_1_submit_occupancy.md` through `experiment_5_submit_admission_probe.md`.
2. Put selector, implementation module, question, setup diagram, measured signal, and notes in each file.
3. Move detailed Experiment 2 rerun/cacheline/page-alignment notes into the Experiment 2 file.
4. Keep `EXPERIMENTS.md` as an index linking the per-experiment docs.
5. Validate with `git diff --check`.

## Completion notes

Completed. Created five per-experiment markdown files and reduced `EXPERIMENTS.md` to an index. Validated with `git diff --check` on the affected markdown files.
