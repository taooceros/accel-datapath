# Experiment 2 no-black-box rerun agent plan

## Reader

Future agents checking why the Experiment 2 polling path was rerun without `core::hint::black_box`.

## Scope

Make one narrow hot-loop code change, verify it, rerun the hardware benchmark, regenerate the latency-order plot, and record the observed result. Do not redesign Experiment 2 or change the JSON contract.

## Steps

1. Replace `let status = core::hint::black_box(poll.value);` with direct use of `poll.value`.
2. Run the targeted Experiment 2 tests and release build.
3. Run the same launch command as the 2026-05-31 Experiment 2 rerun.
4. Regenerate the ordered latency plot from a fixed sample iteration if present.
5. Validate JSON and artifact shape.
6. Write a short benchmarking report and mark the project plan complete.

## Verification

Use the project release build and launch flow. Validate one row, 1000 sample traces, trace length 160, completed/missing/errors medians 160/0/0, and non-empty PNG/CSV output.
