# Experiment 2 all-variant trace agent plan

## Scope

Produce refreshed trace artifacts for the focused Experiment 2 baseline and each mechanism-probe variant.

## Steps

1. Build `hw-eval` in release mode.
2. Run `submit-marker-overlap` with burst 160, poll offset 1, noop, 1000 iterations.
3. Run `submit-marker-mechanism` with the same burst, offset, operation, and iteration count.
4. Generate one PNG and one CSV per trace-capable row using a shared sample iteration.
5. Validate row counts, trace lengths, artifact existence, and summarize the result in a benchmarking report.

## Guardrails

- Use `launch` for hardware runs.
- Do not treat raw multi-row JSON as a committed artifact if it is too large; keep compact CSV/PNG/report artifacts.
- Keep variant labels tied to the JSON row selectors.

## Completion

Completed. Ran overlap baseline and all mechanism rows, generated per-row CSV/PNG traces for sample iteration 804, validated artifact counts and trace shapes, and wrote `docs/report/benchmarking/038.experiment2_all_variant_traces_2026-05-31.md`.
