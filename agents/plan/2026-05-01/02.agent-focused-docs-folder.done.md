# Move agent-focused docs into top-level agents folder

## Goal

Create a visible top-level `agents/` area for agent-focused operating material and move documentation that is primarily for agent workflow/use out of `docs/`.

## Scope

- Add `agents/` as the home for agent workflows, templates, state notes, and agent-specific reports.
- Migrate `docs/report/workflow/` into the new agent-focused tree because those reports describe agent orchestration, task shaping, and live-thread workflow rather than project research results.
- Keep domain research, architecture, benchmarking, hardware, specs, and historical plan records under `docs/`.
- Update active references so future agents find the moved workflow reports in `agents/`.
- Preserve compatibility with the existing `.agents/` content during this pass unless a smaller move would leave duplicated active sources.

## Intended changes

- Create `agents/README.md` and a local `agents/AGENTS.md`.
- Move `docs/report/workflow/*.md` to `agents/report/workflow/`.
- Update `docs/report/AGENTS.md` to remove `workflow/` from the docs report topic map.
- Update references from `docs/report/workflow/...` to `agents/report/workflow/...`.

## Verification

- Search for stale `docs/report/workflow` references.
- Search `docs/report/AGENTS.md` for the old workflow topic entry.
- Inspect `git status --short` and keep unrelated pre-existing changes separate.

## Completion notes

Created the top-level `agents/` area with local placement guidance, moved the agent workflow reports from `docs/report/workflow/` to `agents/report/workflow/`, and updated active docs/report guidance so future agent-orchestration reports go under `agents/` rather than `docs/`.
