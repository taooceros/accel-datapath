# Agent execution plan: idxd boundary cleanup

## Reader

Coding agents working on the fast `idxd-rust` / `idxd-sys` cleanup requested on 2026-05-01.

## Execution shape

- Parent agent stays the single writer for final integration.
- Spawn parallel subagents for quick code reconnaissance only:
  - sys helper inventory;
  - idxd-rust raw usage/wrapper targets;
  - async path compile-risk scan.
- Avoid external research and broad redesign.

## Constraints

- Follow `agents/CODING_REQUIREMENTS.md`.
- Keep `idxd-sys` raw and bindgen-oriented.
- Move Rust helper logic to `idxd-rust`.
- Prefer small typed wrappers and `#[repr(...)]` enums over raw constants in safe code.
- Use narrow host-free verification only.

## Stop rules

Escalate only if the current API shape cannot preserve existing tests without a broader redesign. Otherwise make the smallest compile-safe cleanup.

## Verification target

Run narrow formatting/tests after edits; do not run hardware-facing binaries.
