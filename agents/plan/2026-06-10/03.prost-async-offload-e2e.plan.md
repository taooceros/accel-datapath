# Prost async offload E2E execution plan

## Goal

Implement and verify a Prost-only end-to-end async payload-copy test for the generated resumable `poll_encode` path.

## Scope

- Target `accel-rpc/tonic/prost/tests/src/message_encoding.rs`.
- Reuse the existing async encode test helpers where possible.
- Add only local test fixtures and a fake offload sink; do not touch Tonic codec files.

## Steps

1. Inspect existing async encode tests and `AsyncEncodeSink` state semantics.
2. Add generated test fixtures that cover scalar, plain payload, repeated, map, nested, and oneof payload paths.
3. Add a fake DSA sink that returns `Pending` from `poll_put_payload`, keeps only owned payload state while pending, and copies into the destination on resume.
4. Assert pending count, exact byte equality with synchronous Prost encode, and normal Prost decode equality.
5. Run the requested targeted and full Prost test commands plus `git diff --check`.

## Verification

Use the exact commands requested in the user task from `accel-rpc/tonic/prost`.

## Completion notes

- Added the E2E fixture and fake DSA sink in `tests/src/message_encoding.rs`.
- Verified targeted async encode tests, full Prost/prost-derive/tests suites, and `git diff --check`.
- Did not modify Tonic codec files.
