# Prost DSA test ownership

## Goal
Apply the user's crate-boundary correction: Prost owns generated-message DSA payload-copy tests; `tonic-dsa-bytes-async` owns raw bytes codec DSA behavior.

## Steps
1. Inspect the existing hardware DSA tests and Prost async encode tests.
2. Move generated protobuf hardware coverage into Prost tests with opt-in ignored execution.
3. Keep only raw bytes hardware smoke coverage in `tonic-dsa-bytes-async/tests`.
4. Add small crate-local AGENTS files that state each crate's job.
5. Run targeted cargo tests, including the ignored hardware test through `dsa_launcher` if needed.

## Constraints
- Do not change `tonic/src/codec/encode.rs`.
- Keep DSA opt-in and hardware tests ignored.
- Keep `prost::Message::encode_raw` synchronous.
- Do not add boxed futures or staging production copy paths.

## Verification log
Completed with:
- `PROTOC_INCLUDE=/home/hongtao/accel-datapath/tonic-dsa/accel-rpc/tonic/prost/tests/src/include rtk cargo test -p tests async_encode` from `accel-rpc/tonic/prost`: 7 passed, 62 filtered out.
- `TONIC_DSA_WQ=/dev/dsa/wq0.0 PROTOC_INCLUDE=/home/hongtao/accel-datapath/tonic-dsa/accel-rpc/tonic/prost/tests/src/include rtk ../../../tools/build/dsa_launcher cargo test -p tests --features hardware-dsa dsa_encode_async_matches_sync_encode_for_generated_payloads -- --nocapture` from `accel-rpc/tonic/prost`: 1 passed, 0 ignored; printed `payload_len=1476868 pending_polls=912 dsa_payloads=29 prefix_hex=082a1280c0066861726477617265206473612070726f737420737472696e6720`.
- `TONIC_DSA_WQ=/dev/dsa/wq0.0 PROTOC_INCLUDE=/home/hongtao/accel-datapath/tonic-dsa/accel-rpc/tonic/prost/tests/src/include rtk ../../tools/build/dsa_launcher cargo test -p tonic-dsa-bytes-async -- --nocapture` from `accel-rpc/tonic`: 8 tests passed, 0 ignored.
- `rtk git diff --check` from repo root: passed.
