# Prost real DSA payload-copy execution plan

## Goal

Finish and verify the real DSA-backed generated Prost async payload-copy path without changing Tonic codec encode plumbing.

## Scope

- Inspect the existing Prost `AsyncEncodeSink` and generated `poll_encode_raw` state-machine conventions.
- Reuse the existing async DSA work queue, memmove retry, page touching, completion polling, and drop-drain behavior.
- Keep DSA opt-in and CPU as the default path.
- Keep the hot generated Prost path free of boxed futures and avoid holding borrowed buffer views across `Poll::Pending`.

## Steps

1. Audit the current async Prost sink implementation in `tonic-dsa-bytes-async/src/prost_codec.rs` against the state-machine and buffer-safety requirements.
2. Tighten implementation details if needed so the DSA payload state owns all pending descriptor state and commits destination bytes exactly once.
3. Expand hardware coverage in `tests/hardware_dsa.rs` to encode a comprehensive generated Prost message and compare against synchronous Prost bytes plus decode equality.
4. Preserve existing fake offload and CPU fallback tests.
5. Run the requested targeted Prost, full Prost, DSA async/sync, optional hardware, and whitespace checks.
6. Audit the diff to confirm `tonic/src/codec/encode.rs` was not modified.

## Verification

Use the exact commands listed in `docs/plan/2026-06-10/04.prost-real-dsa-payload-copy.plan.md`.

## Completion notes

- Audited the Prost async generated encode path and existing async DSA byte-copy machinery.
- Kept the DSA implementation behind `tonic-dsa-bytes-async`; generated Prost messages still offload only through `AsyncEncodeSink::poll_put_payload`.
- Fixed the Prost encode future drop-order safety issue by declaring payload state before the owned message and encode buffer sink.
- Added an ignored hardware test for a comprehensive generated Prost payload message and verified it with `tools/build/dsa_launcher`.
- Verified targeted Prost async tests, full local Prost tests, async/sync DSA crate tests, hardware tests, whitespace, and no `tonic/src/codec/encode.rs` diff.
