# Agent plan: neutral bytes benchmark crate

## Goal

Refactor the three-way bytes RPC benchmark into a neutral crate so `tonic-dsa-bytes-async` no longer contains the standard codec baseline.

## Constraints

- Do not disturb the existing `tonic-dsa-bytes-sync` benchmark binary.
- Keep the moved benchmark behavior equivalent.
- Trim only dependencies that were added for the comparison benchmark; keep async crate test dependencies needed by hardware smoke tests.
- Verify with targeted checks and one hardware `launch` smoke run.

## Steps

1. Create `tonic-bytes-bench` and register it in the workspace.
2. Move `compare_bytes_rpc_throughput.rs` into the neutral crate.
3. Move benchmark dependencies from `tonic-dsa-bytes-async` to `tonic-bytes-bench`.
4. Update reports/plans to reference the neutral crate.
5. Run formatting, checks, tests, clippy, release build, async crate checks, and a short hardware benchmark run.

## Completed work

- Created `tonic-bytes-bench` and registered it in the workspace.
- Moved `compare_bytes_rpc_throughput.rs` into `tonic-bytes-bench/src/bin`.
- Removed comparison-benchmark dependencies from `tonic-dsa-bytes-async`.
- Updated `docs/report/benchmarking/054.async_sync_standard_bytes_rpc_2026-06-08.md` and completion notes to reference `tonic-bytes-bench`.
- Verified benchmark crate format/check/test/clippy/release build, async crate check/test/clippy, workspace check excluding `interop`, and a short hardware `launch` run for all three paths.
