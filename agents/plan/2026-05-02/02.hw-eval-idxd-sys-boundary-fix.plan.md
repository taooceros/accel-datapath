# Agent plan: hw-eval idxd-sys boundary fix

## Steps

1. Inspect new `idxd-sys` raw UAPI exports and existing `idxd-rust` thin wrapper patterns.
2. Replace `hw-eval/src/dsa.rs` broken re-export shim with local wrappers backed by `idxd_sys::idxd`.
3. Keep only the DSA helpers needed by `hw-eval` benchmarks: noop, memmove, crc_gen, copy_crc, batch, completion accessors, polling/reset helpers.
4. Run focused hw-eval tests and formatting checks.
5. Commit the fix without including unrelated async benchmark worktree changes.

## Completion notes

Implemented local DSA descriptor/completion wrappers over `idxd_sys::idxd` raw UAPI storage. Verified with `cargo test -p hw-eval --lib --tests`, `cargo fmt -p hw-eval -- --check`, and `git diff --check`.
