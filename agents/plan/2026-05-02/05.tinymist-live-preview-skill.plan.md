# Agent Plan: Tinymist live preview skill

## Goal
Capture the successful Tinymist live preview plus browser automation workflow as a project-local agent skill.

## Scope
- Add a new skill under `.agents/skills/`.
- Keep it focused on creating and visually iterating Typst slide decks using Tinymist preview and browser automation.
- Include the observed local commands and pitfalls from the smoke test.

## Intended changes
- Create `.agents/skills/create-typst-slides-live/SKILL.md`.

## Verification
- Check the skill file exists.
- Check YAML frontmatter name matches the directory.
- Ensure the skill body uses XML sections rather than markdown headings.

## Completion notes
Implemented `.agents/skills/create-typst-slides-live/SKILL.md`. Validation checked that the file exists, frontmatter name matches the directory, required XML sections are present, and there are no markdown headings outside code blocks.
