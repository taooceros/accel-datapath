# Agent plan: hw-eval uses idxd-rust raw wrappers

## Steps

1. Make `idxd-rust::raw` public so downstream benchmark crates can use canonical raw wrappers.
2. Expose the DSA descriptor raw pointer helper needed by `hw-eval`'s local portal submission path.
3. Refactor `hw-eval/src/dsa.rs` into a compatibility shim over `idxd_rust::raw::dsa` instead of local bindgen-backed wrappers.
4. Update DSA methodology calls for canonical raw wrapper signatures.
5. Run focused hw-eval tests and formatting checks.
6. Commit the focused refactor only.

## Caveat

Switching DSA is possible now. IAX is not fully switched in this change because `idxd-rust` currently exposes IAX crc64 raw helpers, while `hw-eval` also needs IAX noop descriptors.

## Completion notes

Implemented for DSA: `hw-eval` now depends on `idxd-rust` and uses `idxd_rust::raw::dsa` canonical wrappers through a compatibility shim. Made `idxd-rust::raw` public and exposed `DsaHwDesc::as_desc64_ptr()` for raw portal submission. Verified with `cargo test -p hw-eval --lib --tests`, `cargo fmt -p hw-eval -- --check`, and `git diff --check`. IAX remains local because `idxd-rust` only has IAX crc64 raw helpers today, while `hw-eval` also needs IAX noop.
