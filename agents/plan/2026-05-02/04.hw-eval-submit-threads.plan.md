# Agent plan: hw-eval submit threads

## Steps

1. Add `--threads` to config with validation that it is greater than zero.
2. Add the value to runtime config and metadata.
3. Pass it into the DSA methodology.
4. Implement a concrete DSA memmove multi-thread direct-descriptor benchmark that runs only when `threads > 1`.
5. Verify with focused tests and a small hardware smoke if available.
6. Commit only the focused hw-eval thread-argument change.

## Completion notes

Implemented `--threads` and a DSA direct memmove multi-thread row family `memmove_mt_t<N>`. Refactored the single-thread and multi-thread direct paths through shared `DirectWindow` state/runner to avoid duplicate descriptor submission/polling loops. Verified with focused tests, release build, formatting checks, and a shared-WQ hardware smoke on `/dev/dsa/wq0.1`.
