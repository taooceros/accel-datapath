# Agent plan: hw-eval sliding-window timing fix

## Steps

1. Read current hw-eval DSA/IAX sliding-window implementations.
2. Move timing start before initial window submission for direct sliding-window benchmarks.
3. Fix DSA pipelined-batch sliding-window accounting to use `window` and `issued_batches`.
4. Run a targeted build/test if possible.
5. Commit only the focused hw-eval timing fix and its plan artifacts, avoiding unrelated existing worktree changes.

## Completion notes

Implemented the DSA/IAX sliding-window timing fix. Ran `cargo fmt -p hw-eval`. Targeted `cargo test -p hw-eval --lib --tests` is currently blocked before reaching this change because `hw-eval/src/dsa.rs` still imports pre-cleanup `idxd-sys` symbols removed by the in-flight raw-boundary redesign.
