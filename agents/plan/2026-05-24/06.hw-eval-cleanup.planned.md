# hw-eval cleanup execution plan

Status: completed
Date: 2026-05-24

## Reader

Coding agent cleaning up `hw-eval` after the submit-only, timer-source, and admission-probe additions.

## Scope

Touch only focused cleanup targets unless verification exposes a direct regression:

- `hw-eval/src/config.rs`
- `hw-eval/src/main.rs`
- `hw-eval/src/report.rs`
- `hw-eval/src/methodology/dsa.rs`

Do not touch unrelated dirty files. Do not run hardware binaries directly. Do not add dependencies.

## Completed steps

1. Preserved CLI and JSON compatibility: benchmark names, timer names, metadata fields, optional latency fields, and admission result fields remain stable.
2. Applied small helper-level cleanup only:
   - lazy allocation in comma-list parsing;
   - centralized timer-row construction in `LatencyResult`;
   - clearer submit-only workload constants;
   - shared completion-status accumulation for admission probing.
   - CLI normalization through the method-level `bon` builder, avoiding a warning suppression;
3. Kept hot measurement loops free of runtime timer dispatch and per-submit instrumentation.
4. Added a focused test for submit-timer legacy alias fields.
5. Verified with `cargo fmt --package hw-eval`, `cargo test -p hw-eval`, and `cargo build --release -p hw-eval`.

## Non-goals

- No hardware re-run.
- No benchmark-methodology changes.
- No WQ reconfiguration.
- No broad split of the large DSA benchmark module.
- No removal of the documented method-level `bon` constructor boundary unless a separate plan revisits that convention.

## Verification result

- `cargo test -p hw-eval`: 24 tests passed.
- `cargo build --release -p hw-eval`: passed.
