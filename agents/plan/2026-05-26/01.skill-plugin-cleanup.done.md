# Skill and plugin cleanup

## Goal

Reduce project/session startup noise by removing skills and plugins that are not useful for this repository's current Rust/C++ DSA/IAX work.

## Scope

- Remove project-local skill links for rarely used Rust meta/learning/graph tooling:
  - `rust-daily`
  - `rust-skill-creator`
  - `meta-cognition-parallel`
  - `m14-mental-model`
  - `rust-deps-visualizer`
  - `rust-trait-explorer`
  - `rust-call-graph`
- Remove user-local duplicate/general C++ skill entry `cpp-pro`, keeping `cpp-expert`.
- Disable unused user plugins:
  - `voltagent-lang`
  - `voltagent-qa-sec`
  - `voltagent-meta`
  - `voltagent-research`
  - `pyright-lsp`
- Move unrelated global GSD/user-home skills out of active `skills/` directories while keeping focused engineering skills available.

## Verification

- Confirm the removed project-local skill paths are absent.
- Confirm `settings.json` no longer enables the disabled plugins.
- Preserve Rust analyzer and clangd plugin enablement.
- Confirm global user-home `cpp-pro` markers are absent from active agent skill roots.

## Completion notes

- Removed selected project-local Rust meta/graph skills from `.claude/skills`.
- Disabled `voltagent-*` and `pyright-lsp` in `/home/hongtao/.claude/settings.json`.
- Removed duplicate `cpp-pro` from user-home active agent skill roots.
- Moved unrelated global harness skills from `/home/hongtao/.agents/skills` to `/home/hongtao/.agents/skills.disabled-cleanup-2026-05-26` so they are reversible but not active.