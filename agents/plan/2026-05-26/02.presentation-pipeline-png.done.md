# Presentation pipeline PNG inspection plan

## Goal

Update the presentation subtree guidance so agents use Typst PNG exports as the normal visual inspection artifact while iterating on slide decks, and reserve PDF export for the final deliverable stage.

## Scope

- Modify `presentation/AGENTS.md` only.
- Keep the existing slide-planning and design guidance intact.
- Clarify that PNG export is for agent visual inspection, not a substitute for final PDF output.

## Intended change

Add a presentation workflow rule requiring agents to:
1. Compile Typst decks to PNG during iteration, preferably with deterministic page filename templates.
2. Inspect the generated PNG pages for layout/readability.
3. Export the PDF only at the final stage or when explicitly requested for PDF-specific verification.

## Verification

- Read the changed `presentation/AGENTS.md` section to confirm the guidance is present and unambiguous.

## Completion notes

Completed. `presentation/AGENTS.md` now directs agents to use Typst PNG exports for iterative visual inspection and reserve PDF export for final deliverables or PDF-specific checks.