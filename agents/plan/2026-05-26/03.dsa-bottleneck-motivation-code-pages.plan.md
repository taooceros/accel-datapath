# DSA bottleneck deck motivation/code page plan

## Goal

Revise `presentation/2026-05-26/dsa_submission_bottleneck_experiments.typ` so each numbered experiment has a companion page explaining motivation, learning goal, and a compact Rust prototype rendered with `zebraw`.

## Scope

- Update the existing human slide plan with the added companion pages.
- Modify only the current Typst deck under `presentation/2026-05-26/`.
- Preserve the current experiment diagrams and attribution framing.
- Add `zebraw` as a presentation dependency for code rendering.

## Intended change

For experiments 1 through 5, insert one page immediately after the experiment's diagram/readout page. Each page should contain:

1. Motivation: why this experiment exists.
2. What we are trying to learn: the discriminating outcome.
3. A simple Rust code sketch showing the loop shape and recorded variables.

## Verification

- Compile the deck to PNG with Typst to catch syntax/layout-level errors.
- Inspect generated PNG page metadata/listing enough to confirm pages were emitted.
- Export the final PDF after the PNG compile succeeds.

## Completion notes

Completed. Added four companion motivation/prototype pages, imported `zebraw`, updated the human slide plan, compiled PNG previews, visually checked the new code-heavy pages, rebuilt the final PDF, later removed the out-of-scope CPU-load experiment, and then added a new dedicated-WQ blind-push loss-check experiment with its own simple Rust sketch.