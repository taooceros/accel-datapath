# Consolidate DSA experiment slide plan

## Goal

Rewrite the current human slide plan so it describes the latest DSA submission-bottleneck experiment design directly, instead of preserving obsolete earlier slide sequences plus appended revisions.

## Scope

- Compare `docs/plan/2026-05-26/01.dsa-submission-bottleneck-experiment-slide.plan.md` with `presentation/2026-05-26/dsa_submission_bottleneck_experiments.typ`.
- Keep the plan as the source-facing design record for the current deck.
- Do not change the Typst deck unless the comparison shows presentation content that is clearly wrong.

## Intended change

- Replace the layered historical plan with one consolidated 16-slide plan matching the deck:
  - framing slides,
  - DSA loop and traffic classes,
  - five experiment pages,
  - five motivation/Rust sketch companion pages,
  - attribution rule.
- Preserve traceable source claims and keep the document reader-facing.

## Verification

- Re-read the updated plan and deck headings to confirm slide count, sequence, and visible components match.
- If Typst is not changed, do not rebuild the PDF; the existing compiled deck remains unaffected.

## Completion notes

Completed. The human slide plan now matches the current presentation sequence and records the comparison result.