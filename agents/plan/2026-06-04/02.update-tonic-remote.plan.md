# Agent plan: sync vendored Tonic fork with upstream

## Goal

Advance `accel-rpc/tonic` and the fork remote `taooceros/tonic` to the latest upstream `grpc/grpc-rust` commit without disturbing unrelated repository changes.

## Steps

1. Record current submodule commit and remotes.
2. Add/fetch upstream `https://github.com/grpc/grpc-rust`.
3. Fast-forward local `master` to `upstream/master`.
4. Push local `master` to fork `origin/master`.
5. Confirm submodule worktree cleanliness and parent pointer change.
6. Run targeted verification from `accel-rpc`.
7. Update the human plan completion notes.

## Constraints

- Treat all pre-existing parent-repo changes as user work.
- Do not stash, reset, or clean.
- Do not edit vendored Tonic source.
- Use Cargo-native verification.

## Verification

- `git -C accel-rpc/tonic status --short --branch`
- `cargo check` with `cwd=accel-rpc`

## Completion notes

Completed. Added/fetched upstream `https://github.com/grpc/grpc-rust`, fast-forwarded `accel-rpc/tonic` `master` from `4f57dd466984de5a3bf394b9977d920b3137d779` to upstream `4687108a52b1080c53a87a323f8bbd50b71f6d43`, and pushed the same commit to fork `origin/master`. Submodule status is clean at `master...origin/master`; parent repository now shows a modified `accel-rpc/tonic` gitlink. `cargo check` in `accel-rpc` was attempted and failed on existing `tonic-profile` / `idxd-rust` API drift, not on the Tonic sync.
