# Refresh codemogger skill for MCP workflow

## Goal

Update the repo-local `codemogger` skill so it reflects the current MCP server behavior instead of the older CLI-only workflow.

## Scope

- revise `.agents/skills/codemogger/SKILL.md`
- document the current `codemogger_search`, `codemogger_index`, and `codemogger_reindex` tools
- clarify semantic vs keyword usage, indexing triggers, and maintenance guidance
- keep guidance aligned with root `AGENTS.md` retrieval rules and `tursodb-kb` boundaries

## Plan

1. compare the current skill text against the MCP server implementation
2. update the skill workflow, tool boundary, and output contract
3. verify the revised guidance is internally consistent and matches the server code

## Result

- updated `.agents/skills/codemogger/SKILL.md` to reflect MCP-tool usage instead of the older CLI-only workflow
- documented `codemogger_search`, `codemogger_index`, and `codemogger_reindex`, including semantic vs keyword guidance and default parameters
- aligned maintenance, workflow, and stop conditions with the current server behavior
