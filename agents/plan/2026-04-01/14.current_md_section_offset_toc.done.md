# Section-and-offset TOC for current.md

## Goal

Refine `current.md` so the TOC lists only section status, section name, and starting offset, making it faster for agents to jump to the right part of the file.

## Scope

- update root `AGENTS.md`
- update root `current.md`

## Planned changes

1. Require TOC entries in `current.md` to list only status, section name, and offset.
2. Remove anchor links from the TOC format guidance.
3. Update `current.md` to use the new section-and-offset TOC format.

## Completion notes

- Root `AGENTS.md` now requires TOC entries that list only section status, section name, and starting offset.
- `current.md` TOC entries now use the section-and-offset format for faster line-based navigation.
