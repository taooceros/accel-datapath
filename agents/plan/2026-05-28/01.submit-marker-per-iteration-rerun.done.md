# Submit marker per-iteration rerun

## Goal
Rerun Experiment 2 after changing `sample_trace` from one midpoint trace to per-iteration traces, then regenerate the trace plot from one randomly selected iteration.

## Scope
- Keep the hardware run focused on the existing Experiment 2 offset-1 noop setup used by the latest report.
- Update the plotting script to consume per-iteration `sample_trace` rows and randomly choose one iteration by default.
- Regenerate the JSON, CSV, and PNG artifacts from the new hardware data.
- Record the selected random iteration and validation results in a benchmarking report.

## Verification
- Release-built `hw-eval` before hardware execution.
- Ran the hardware benchmark through `launch`.
- Checked JSON shape: one result row, per-iteration sample trace count equals iterations, each selected trace has N points.
- Ran plot generation and confirmed output files are non-empty.

## Completion notes
- Hardware rerun produced `marker_trace_per_iteration_offset1_noop.raw` and clean JSON.
- Plot script selected random iteration 785 and generated `submit_poll_trace_per_iteration_offset1_noop.png` plus CSV.
- Durable report: `docs/report/benchmarking/027.submit_marker_per_iteration_trace_2026-05-28.md`.
