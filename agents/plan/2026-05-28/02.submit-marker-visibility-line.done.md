# Submit marker visibility line

## Goal
Mark the first submit index in a randomly selected Experiment 2 iteration where the single completion-status read observes a visible completion.

## Scope
- Preserve the lean hot-loop measurement: one status read, one latency measurement.
- Carry the read status into the per-iteration sample point so plotting can find first visibility.
- Draw a vertical line on the regenerated plot at the first visible completion in the selected iteration.
- Rerun focused hardware data and regenerate JSON/CSV/PNG artifacts.

## Verification
- Targeted Rust tests for sample point status propagation.
- Release build before hardware run.
- Hardware rerun through `launch`.
- JSON/CSV validation that the plotted iteration has a first visible-completion index.

## Completion notes
- Added sampled marker status to each per-iteration trace point.
- Plot now draws a vertical guide for every submit index where the sampled status read observed a visible completion.
- Regenerated hardware JSON/CSV/PNG; plotted iteration 538 has visible reads at submit indices 7..160.
