# Submit-occupancy huge-memmove execution plan

## Reader

Future coding agent or reviewer checking why the `hw-eval` CLI gained a custom memmove payload option for the Experiment 1 run.

## Scope

- Inspect current `DsaOperationClass` plumbing and submission-bottleneck payload selection.
- Add the smallest compatible configuration surface for a 16 MiB memmove payload.
- Update host-free CLI/config contract tests and user-facing README text if needed.
- Build and run the focused hardware command through `launch`.
- Write a benchmark report with commands, artifacts, and interpretation.

## Non-goals

- Do not redesign the submission-bottleneck experiment tree.
- Do not change existing JSON row names unless a custom payload needs a distinguishable label.
- Do not run hardware binaries directly outside the launcher flow.

## Verification checklist

- Focused Rust tests for CLI/config parsing.
- `cargo build --release -p hw-eval`.
- Launch-wrapped `submit-occupancy` run on `/dev/dsa/wq0.0`.
- JSON sanity check for missing/error counts and knee behavior.
