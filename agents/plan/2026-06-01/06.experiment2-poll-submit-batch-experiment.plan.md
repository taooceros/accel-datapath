# Experiment 2 poll-submit-batch execution plan

## Goal

Implement and run the Experiment 2 mechanism probe for polling after every N submitted requests.

## Scope

- Touch `hw-eval` config, benchmark dispatch, mechanism probe result construction, and report schema as needed.
- Use `poll_submit_batch_n` for the submit-side polling interval. Do not call this DSA batch size.
- Keep completion-read timing mode separate as `timing_mode`.
- Run hardware only through `launch`.

## Steps

1. Add `--marker-poll-submit-batches` parsing and config plumbing.
2. Add focused `poll-submit-batch` mechanism rows and result field.
3. Run tests and release build.
4. Run focused hardware benchmark.
5. Analyze JSON and write report.

## Verification

Use `cargo fmt -p hw-eval`, targeted Experiment 2 tests, full `cargo test -p hw-eval`, release build, and a focused `launch ./target/release/hw-eval ...` run.

## Completion notes

Pending.
