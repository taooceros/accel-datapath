# Experiment 2 configuration-suite agent plan

## Scope

Change the default Experiment 2 route so `submit-marker-overlap` emits the baseline trace rows and all mechanism-probe rows in one run. Keep mechanism-only runs available.

## Steps

1. Update `submission_bottleneck::run` so the `SubmitMarkerOverlap` branch runs `bench_submit_marker_overlap` and then `bench_submit_marker_mechanism_probes`.
2. Update CLI-facing text/docs so users know `submit-marker-overlap` is now the full Experiment 2 suite and `submit-marker-mechanism` is mechanism-only.
3. Add or update tests that anchor the supported selectors and DSA-only validation.
4. Verify with targeted tests, release build, and a tiny hardware smoke run checking both result arrays.

## Guardrails

- Do not change JSON row schemas.
- Do not remove the mechanism-only selector.
- Keep the broad matrix in operator-controlled CLI knobs; the default suite combines sub-experiments, not every possible Cartesian product of every knob.

## Completion

Completed. The `SubmitMarkerOverlap` route now emits both overlap and mechanism result families, CLI docs describe it as the default Experiment 2 suite, and verification passed with `cargo test -p hw-eval`, release build, and hardware smoke validation.
