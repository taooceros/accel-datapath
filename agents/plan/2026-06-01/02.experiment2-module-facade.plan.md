# Experiment 2 module facade execution plan

## Goal

Refactor Experiment 2 so the outer Rust module is a facade that exports benchmark entry methods and does not contain implementation details.

## Scope

- `hw-eval/src/benchmarks/submission_bottleneck/experiment_2_marker_overlap.rs`
- `hw-eval/src/benchmarks/submission_bottleneck/experiment_2_marker_overlap/`
- `hw-eval/src/benchmarks/submission_bottleneck.rs`

## Steps

1. Move the current overlap implementation into a private child module.
2. Replace the outer module body with private module declarations and `pub(crate) use` exports for both Experiment 2 entry methods.
3. Update the parent dispatcher import to use the facade only.
4. Run targeted Rust tests/checks that compile the affected crate paths.

## Verification

Use cargo tests for `hw-eval` CLI/module coverage. Do not run hardware benchmarks directly.

## Notes

Preserve behavior and result schema; this is an organization-only cutover.

## Completion notes

Implemented as an organization-only cutover. Verified with `cargo test -p hw-eval` and `cargo fmt -p hw-eval --check`.
