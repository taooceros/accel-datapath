# Experiment 1 codepath cleanup execution plan

## Target

Clean up `hw-eval/src/benchmarks/submission_bottleneck/experiment_1_submit_occupancy.rs` after the raw trace-mode changes.

## Constraints

- Do not add a separate report top-level for trace mode.
- Keep trace data inside existing `submit_occupancy` rows via `extra_submit_trace` and `trace_outcomes`.
- Do not add p95 or other plot-only statistics to Rust; Python post-processing owns those.
- Keep allocations outside measured submit loops.
- Preserve the launch-only hardware execution convention.

## Steps
1. Refactor Experiment 1 around explicit scratch/case/result helpers.
2. Keep reset/prefill/timed-submit/drain shared through scratch code while removing runner-side summary/statistics mode.
2. Keep normal one-extra and trace mode sharing reset/prefill/drain code.
3. Build and run focused tests.
4. Run launch smoke for both JSON shapes.

## Acceptance

The experiment code should be easier to follow without changing the user-facing command shape. Summary mode and trace mode must both run through `launch` successfully.
