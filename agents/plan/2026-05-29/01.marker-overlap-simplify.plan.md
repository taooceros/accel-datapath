# Marker overlap simplification execution plan

## Goal

Refactor `experiment_2_marker_overlap.rs` locally for clarity while preserving behavior and output compatibility.

## Steps

1. Inspect the current file around helper and loop structure.
2. Replace duplicated timing plumbing with small local helpers/types.
3. Add a local `measured_call` wrapper for TSC-bracketed submit/status-read calls.
4. Keep hot-loop work equivalent: submit timing, optional frontier polling, trace/sample recording.
5. Run formatting and focused tests.

## Verification

- `cargo fmt -p hw-eval`
- `cargo test -p hw-eval marker_overlap`
- `git diff --check -- hw-eval/src/benchmarks/submission_bottleneck/experiment_2_marker_overlap.rs docs/plan/2026-05-29/01.marker-overlap-simplify.plan.md agents/plan/2026-05-29/01.marker-overlap-simplify.plan.md`

## Outcome

Completed a local simplification with no intentional benchmark contract changes.
