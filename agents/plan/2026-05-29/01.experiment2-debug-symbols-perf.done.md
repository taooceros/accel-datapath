# Experiment 2 debug symbols and perf profile

## Goal
Build `hw-eval` release binaries with DWARF debug symbols and run a quick perf profile of Experiment 2 to identify extra CPU-side overhead in the current next-unfinished polling path.

## Scope
- Added release-profile debug symbols without changing optimization level.
- Rebuilt `hw-eval` release.
- Ran Experiment 2 through `launch perf record` using the focused noop/poll-offset setup.
- Generated stdio perf reports and summarized hotspots in a benchmarking report.

## Verification
- Confirmed release ELF contains `.debug_info`, `.debug_line`, and `.debug_abbrev`.
- Targeted tests still pass.
- Perf data/report artifacts are non-empty and all samples are from `hw-eval`.

## Completion notes
- Durable report: `docs/report/benchmarking/029.experiment2_perf_debug_symbols_2026-05-29.md`.
- Main avoidable overhead found: per-poll/sample trace `Vec` allocation/clone paths and post-run stats sorting.
