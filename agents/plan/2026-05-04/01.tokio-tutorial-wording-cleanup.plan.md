<!-- Superseded: read agents/plan/2026-05-04/07.tokio-general-tutorial-consolidated.plan.md first. This file is retained for historical revision details. -->

# Agent plan: Tokio tutorial wording cleanup

## Reader

Future coding/presentation agent continuing this slide cleanup.

## Task

Edit `presentation/2026-05-02/tokio_general_tutorial.typ` to remove slide-visible meta/planning language and make the wording audience-facing.

## Steps

1. Use the existing full-file read anchors for `tokio_general_tutorial.typ`.
2. Apply minimal anchored edits:
   - title slide text and opening callout;
   - all `Takeaway:` callouts;
   - notes/callouts that describe deck intent rather than mechanism;
   - precise `tokio::spawn` contract wording.
3. Avoid changing slide order, diagrams, or visual structure unless a text line requires it.
4. Compile the deck with `typst compile --root presentation ...` to check syntax.
5. Report changed file and verification result.

## Verification

Run:

```bash
typst compile --root presentation presentation/2026-05-02/tokio_general_tutorial.typ /tmp/tokio_general_tutorial_cleanup.pdf
```

The expected useful evidence is whether the file compiles and whether the PDF is produced. A pre-existing non-fatal Typst/package warning may remain.

## State

Completed. The deck wording was cleaned up and the deck was compiled to `/tmp/tokio_general_tutorial_cleanup.pdf`; output has 23 pages with the same non-fatal Typst/mmdr warning observed before cleanup.
