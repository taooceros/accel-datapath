# Agent plan — add C async composition slide

## Reader

Future agent continuing the Tokio general tutorial slide deck update.

## Task

Implement the user request to add a Niebler-inspired transition slide after the existing C callback slide and before the Go/Rust comparison.

## Steps

1. Read the consolidated plan and relevant deck source.
2. Add one slide under `Problem 2` after `C callbacks make the continuation explicit`; use the concrete `read + timeout + CPU-pool parse + event-loop report` example to show the composition problem.
3. Keep sender/receiver discussion conceptual and lightweight: no `stdexec` deep dive and no hardware/project-specific claims.
4. Update the consolidated plan so the current deck structure includes the new transition.
5. Compile the deck and check PDF page count.
6. If claiming visual layout quality, use Tinymist live preview and browser screenshot evidence.

## Verification command

```bash
typst compile --root presentation presentation/2026-05-02/tokio_general_tutorial.typ /tmp/tokio_general_tutorial_check.pdf
pdfinfo /tmp/tokio_general_tutorial_check.pdf | grep Pages
```

## Execution notes

Completed on 2026-05-04. Added overview slide `Example: read + timeout + parse crosses APIs`, follow-up slide `The glue is a hand-written protocol`, and bridge slide `Why Go/Rust/C++ APIs help`. The deck compiles to 32 pages with the known non-fatal `mmdr` warning. Tinymist preview showed the overview as page 7/32, glue as page 8/32, and async API bridge as page 9/32. Screenshots: `/tmp/tokio_manual_async_slide.png`, `/tmp/tokio_manual_async_glue_slide.png`, `/tmp/tokio_async_api_help_slide.png`.
