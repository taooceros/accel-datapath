# hw-eval codepath investigation

## Goal

Orchestrate a focused investigation of the current `hw-eval` codepath and identify concrete improvement opportunities without disturbing unrelated in-flight user changes.

## Scope

- Inspect current branch state and local `hw-eval` conventions.
- Fan out read-only audits across the CLI/report path, the DSA benchmark execution path, and the submission-bottleneck experiment modules.
- Synthesize only source-grounded findings.
- Apply only small, clearly correct fixes.

## Verification

Observed passing:

- `cargo fmt -p hw-eval`
- `cargo test -p hw-eval --test cli_contract`
- `cargo check -p hw-eval`
- `cargo build --release -p hw-eval`
- `cargo test -p hw-eval`
- Scoped `git diff --check` over touched files.
- LSP diagnostics OK for touched Rust files.

## Completion notes

Completed. Applied config validation, README documentation, DSA transfer-size/alignment checks, submit-marker dispatch cleanup, and Experiment 1 sentinel descriptor reuse. Left DSA batch status/page-fault semantics as documented follow-up requiring spec/hardware confirmation.
