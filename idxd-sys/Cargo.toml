[package]
name = "idxd-sys"
version = "0.1.0"
edition = "2021"
description = "Low-level Intel IDXD hardware bindings — bindgen-backed idxd UAPI plus DSA submission helpers"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.69"
