Goal: Implement an opt-in async Prost encoding path that adds `Message::encode_raw_async` beside the existing synchronous `encode_raw`, routes generated string/bytes field encoding through async helper functions, and lets large string/bytes payload copies use an async
 Intel DSA copy when the experimental codec policy selects hardware.

   Constraints:
   - Preserve existing `Message::encode`, `encode_raw`, `tonic-prost`, and default CPU behavior.
   - Keep Tonic framing/body state unchanged; use Tonic’s existing owned encode future boundary.
   - Keep DSA-specific code inside the experimental DSA codec crate.
   - CPU still encodes protobuf structure, keys, lengths, scalars, and field order.
   - Only string/bytes payload copy operations may become async DSA work.
   - Do not silently CPU-fallback after DSA has been selected; selected DSA failure must surface as an encode error.
   - Do not claim performance until correctness and hardware smoke tests pass.

   Acceptance:
   - Derived messages expose async encode behavior equivalent to current sync encoding on the CPU path.
   - Large string/bytes fields can return `Poll::Pending` during encode and resume after DSA completion.
   - Encoded output is byte-for-byte equivalent to normal Prost for CPU/no-DSA cases.
   - Hardware smoke confirms large protobuf string/bytes fields encode and decode correctly through the async DSA path.

---

<!-- Hidden continuation steer. role=user, suppressed from visible transcript. -->

Continue work on the active goal.

<objective>
Goal: Implement an opt-in async Prost encoding path that adds `Message::encode_raw_async` beside the existing synchronous `encode_raw`, routes generated string/bytes field encoding through async helper functions, and lets large string/bytes payload copies use an async
 Intel DSA copy when the experimental codec policy selects hardware.

   Constraints:
   - Preserve existing `Message::encode`, `encode_raw`, `tonic-prost`, and default CPU behavior.
   - Keep Tonic framing/body state unchanged; use Tonic’s existing owned encode future boundary.
   - Keep DSA-specific code inside the experimental DSA codec crate.
   - CPU still encodes protobuf structure, keys, lengths, scalars, and field order.
   - Only string/bytes payload copy operations may become async DSA work.
   - Do not silently CPU-fallback after DSA has been selected; selected DSA failure must surface as an encode error.
   - Do not claim performance until correctness and hardware smoke tests pass.

   Acceptance:
   - Derived messages expose async encode behavior equivalent to current sync encoding on the CPU path.
   - Large string/bytes fields can return `Poll::Pending` during encode and resume after DSA completion.
   - Encoded output is byte-for-byte equivalent to normal Prost for CPU/no-DSA cases.
   - Hardware smoke confirms large protobuf string/bytes fields encode and decode correctly through the async DSA path.
</objective>

Budget:
- Tokens used: 468011
- Token budget: none
- Tokens remaining: unbounded
- Time used: 1181 seconds

This is an autonomous continuation. The objective persists across turns; do not redefine success around a smaller, easier, or already-completed subset.

Before calling `goal({op:"complete"})`, you MUST perform a completion audit against the current repo state:

1. **Restate the objective as concrete deliverables.** What files, behaviors, tests, gates, or artifacts must exist for the objective to be true? Write them down (todo, or in your reasoning).
2. **Map each deliverable to evidence.** For every requirement, identify the authoritative source that would prove it: a file's contents, a command's output, a test's pass status, a PR/issue state.
3. **Inspect the actual current state.** Read the files. Run the commands. Check the tests. Do not rely on memory of earlier work in this session — the repo may have changed.
4. **Match verification scope to claim scope.** A narrow check (one file passes its unit test) does not prove a broad claim (the feature works end-to-end).
5. **Treat uncertainty as not-yet-achieved.** Indirect evidence, partial coverage, missing artifacts, or "looks right" without inspection mean continue working. Gather stronger evidence or do more work.
6. **Budget exhaustion is not completion.** Do not call complete merely because tokens are nearly out. If the budget is tight and the work is unfinished, leave the goal active and stop the turn — the user or runtime decides next steps.

Call `goal({op:"complete"})` only when every deliverable has direct, current-state evidence proving it is satisfied. The completion call is a load-bearing claim; it ends the autonomous loop and surfaces a "done" report to the user.

If the work is not done, just keep working. Do not narrate that you are continuing — execute.

---

for small type that cannot benefits from dsa or other accelerator, encode_async should just ref to the sync encode

---

Do not introduce Box, Pin<Box<dyn Future>>, or Box::pin to make Prost/Tonic async Rust compile when the surrounding codebase is unboxed. Preserve the zero-allocation style: use async fn/RPITIT where accepted, an associated future type, or redesign the trait boundary. If
 boxing is truly necessary, stop and justify the allocation and dynamic dispatch before changing code.

---

let's try the poll based api

---

<system-notice>
Continue. Keep going from where you left off.

- You MUST resume the most recent intent and carry the unfinished work to completion.
- Interrupted mid-step? Pick it back up from where it stopped.
- You NEVER pause to summarize progress, re-confirm the plan, or ask whether to proceed — just continue.
</system-notice>

---

。

---

<system-notice>
Continue. Keep going from where you left off.

- You MUST resume the most recent intent and carry the unfinished work to completion.
- Interrupted mid-step? Pick it back up from where it stopped.
- You NEVER pause to summarize progress, re-confirm the plan, or ask whether to proceed — just continue.
</system-notice>

---

show me what it is looking like

---

I think this api has quite a few contract about how to use the poll_put_raw as it is at the same object as the buf? would you think that returning a dedicated future holding the reference (or own the buffer until finished) is better

---

why do we need self-reference? we are expressing this as owned type?

---

why is self-referencial a problem

---

how does usual rust future handle self referencial

---

A manual future can borrow a buffer, or own a buffer, but it should not store another future that borrows its own buffer. For that, flatten the child future into your own state machine and use poll_* APIs directly.

---

what's the state

---

learn from how EncodeBody is designed, and maybe adopt something similar to that